---
phase: 27-prompt-optimizer-unified-prompt-management
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - backend/app/schemas/prompt.py
  - backend/app/services/prompt_registry.py
  - backend/app/api/prompts.py
  - backend/tests/test_prompts_management_api.py
autonomous: true
requirements: [PROMPT-03, PROMPT-04, PROMPT-05]

must_haves:
  truths:
    - "Admin can list all registered prompts and read one with its active version"
    - "Saving an edit creates a new PromptVersion (source=manual) and can be activated"
    - "Every optimize call records a PromptOptimizationRun (original, result, mode, model, time)"
    - "Adopting an optimization run creates a new version (source=optimized) linked to the run"
    - "Activating an older version performs a rollback without deleting history"
    - "is_system prompts cannot be deleted"
  artifacts:
    - path: "backend/app/api/prompts.py"
      provides: "list/detail/versions/runs/activate/adopt + optimize-with-run-recording endpoints"
      contains: "versions"
    - path: "backend/app/services/prompt_registry.py"
      provides: "create_version, activate_version, record_optimization_run, adopt_run services"
      exports: ["create_version", "activate_version", "record_optimization_run"]
    - path: "backend/app/schemas/prompt.py"
      provides: "Pydantic v2 request/response schemas for prompt management"
      contains: "class PromptResponse"
  key_links:
    - from: "backend/app/api/prompts.py"
      to: "prompt_registry service functions"
      via: "await service calls"
      pattern: "record_optimization_run|activate_version|adopt_run"
    - from: "record_optimization_run"
      to: "PromptOptimizationRun"
      via: "insert run row referencing base_version_id"
      pattern: "PromptOptimizationRun\\("
---

<objective>
Build the Prompt management REST API on top of the registry (Plan 01) and optimizer
(Plan 02): list/detail, versioned edits, version history, activation/rollback, optimization
run recording, and adopt-run-as-new-version. This is where the optimization PROCESS (F3),
versioning + history (F4), and the API surface for the management UI (F5) come together.

Purpose: Expose full lifecycle control of every prompt while preserving immutable history.
Output: prompt schemas, registry service methods, management endpoints, unit tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-prompt-optimizer-unified-prompt-management/27-RESEARCH.md
@backend/app/services/prompt_registry.py
@backend/app/api/prompts.py
@backend/app/utils/pagination.py
@backend/app/utils/exceptions.py
# Follow CRUD + service-layer conventions from an existing router
@backend/app/api/rubrics.py
@backend/app/services/rubric_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + read endpoints</name>
  <files>backend/app/schemas/prompt.py, backend/app/api/prompts.py, backend/app/services/prompt_registry.py</files>
  <behavior>
    - GET /prompts returns list (key, name, category, active version_no, updated_at, last_optimized_at)
    - GET /prompts/{key} returns template + active version content + variables
    - GET /prompts/{key}/versions returns the version chain (newest first)
    - GET /prompts/{key}/runs returns optimization runs (newest first)
    - Unknown key returns 404 via NotFoundException
  </behavior>
  <action>
    Add Pydantic v2 schemas (ConfigDict(from_attributes=True)). Add read service functions to
    prompt_registry. Add the four GET endpoints to prompts.py with STATIC paths before any
    parameterized ones (e.g. keep /optimize above /{key}). Use the generic PaginatedResponse
    where a list can grow.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prompts_management_api.py -k "read or list or versions or runs" -q</automated>
  </verify>
  <done>Read endpoints return correct shapes; unknown key 404s; route ordering keeps /optimize reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Versioning, activation, adopt + run recording</name>
  <files>backend/app/api/prompts.py, backend/app/services/prompt_registry.py</files>
  <behavior>
    - PUT /prompts/{key} creates a new version (source=manual, version_no incremented, parent=current active) and activates it
    - POST /prompts/{key}/activate/{version_no} switches the active version (rollback), history preserved
    - POST /prompts/{key}/optimize records a PromptOptimizationRun (base_version_id=current active, result_content, mode, model, status) and returns the run id + optimized text — still does not change the active version
    - POST /prompts/{key}/adopt {run_id} creates a new version (source=optimized|iterate) from the run's result_content, links run.resulting_version_id, and activates it
    - DELETE on an is_system template returns 409/forbidden
  </behavior>
  <action>
    Implement create_version, activate_version, record_optimization_run, adopt_run in
    prompt_registry (transactional; enforce single active version per template). Wire the
    optimize endpoint from Plan 02 to persist a run here (the stateless variant remains for
    ad-hoc use; the {key}/optimize variant records). Return 201 on version creation, 200 on activate.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prompts_management_api.py -q</automated>
  </verify>
  <done>Edit/activate/adopt/optimize-record all work; exactly one active version invariant holds; is_system protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Management API tests (100%)</name>
  <files>backend/tests/test_prompts_management_api.py</files>
  <behavior>
    - Cover list/detail/versions/runs, PUT new version, activate rollback, optimize records a run
      (mock optimizer client), adopt creates linked version, is_system delete rejected, 404 paths
  </behavior>
  <action>
    Write pytest-asyncio tests against the ASGI app (httpx ASGITransport per conftest), mocking
    prompt_optimizer_client.optimize_prompt. Assert the single-active-version invariant and the
    run→version linkage.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prompts_management_api.py --cov=app.api.prompts --cov=app.services.prompt_registry --cov=app.schemas.prompt --cov-report=term-missing -q</automated>
  </verify>
  <done>All management API tests pass at 100% coverage of the touched modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → management API | Authenticated admin mutates prompt versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Elevation of privilege | mutation endpoints | mitigate | All write endpoints require the admin auth dependency |
| T-27-11 | Tampering | version chain | mitigate | Versions are append-only; edits create new rows, never mutate historical content |
| T-27-12 | Repudiation | who changed a prompt | mitigate | created_by recorded on every version and run |
| T-27-13 | Info disclosure | optimizer key/model | accept | Model name recorded on run is non-secret; API keys never returned |
</threat_model>

<verification>
- `pytest tests/test_prompts_management_api.py` passes at 100% coverage.
- Manual: PUT then activate an older version restores prior content; runs list shows recorded optimization.
</verification>

<success_criteria>
Full prompt lifecycle (list, edit→new version, rollback, optimize→run, adopt→version) is
available over REST with immutable history and admin-only writes; tests pass at 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/27-prompt-optimizer-unified-prompt-management/27-04-SUMMARY.md`.
</output>
