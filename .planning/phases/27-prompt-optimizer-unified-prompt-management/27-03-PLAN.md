---
phase: 27-prompt-optimizer-unified-prompt-management
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/services/prompt_builder.py
  - backend/app/services/scoring_engine.py
  - backend/app/services/skill_conversion_service.py
  - backend/app/services/dry_run_engine.py
  - backend/app/services/conference_prompt_config.py
  - backend/tests/test_prompt_builder_registry.py
autonomous: true
requirements: [PROMPT-02]

must_haves:
  truths:
    - "Every builder reads its base template from get_prompt(key) instead of a hardcoded string"
    - "With no DB override, builder output is byte-identical to the pre-migration output"
    - "An admin-set active version changes builder output without code changes"
  artifacts:
    - path: "backend/app/services/prompt_builder.py"
      provides: "hcp.system + key_message.detection sourced from registry"
      contains: "get_prompt"
    - path: "backend/app/services/scoring_engine.py"
      provides: "scoring.base sourced from registry"
      contains: "get_prompt"
    - path: "backend/tests/test_prompt_builder_registry.py"
      provides: "snapshot regression proving output unchanged with defaults"
      contains: "def test_"
  key_links:
    - from: "backend/app/services/prompt_builder.py"
      to: "prompt_registry.get_prompt"
      via: "async resolve then render placeholders"
      pattern: "get_prompt\\("
    - from: "backend/app/services/scoring_engine.py"
      to: "prompt_registry.get_prompt"
      via: "resolve scoring.base template"
      pattern: "get_prompt\\("
---

<objective>
Migrate all builder call sites from hardcoded prompt strings to the registry resolver
`get_prompt(key)`, rendering the resolved template with the existing dynamic values.
Because the seeded defaults equal the original strings, output must be unchanged — proven
by snapshot regression tests.

Purpose: Make every prompt admin-overridable through the registry (F2) with zero behavior
drift.
Output: refactored builders + snapshot regression tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-prompt-optimizer-unified-prompt-management/27-RESEARCH.md
# Registry resolver + defaults created in Plan 01
@backend/app/services/prompt_registry.py
@backend/app/services/prompt_defaults.py
@backend/app/services/prompt_builder.py
@backend/app/services/scoring_engine.py
@backend/app/services/skill_conversion_service.py
@backend/app/services/dry_run_engine.py
@backend/app/services/conference_prompt_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot baselines (RED)</name>
  <files>backend/tests/test_prompt_builder_registry.py</files>
  <behavior>
    - Capture current output of build_hcp_system_prompt, build_scoring_prompt,
      build_key_message_detection_prompt, build_conference_audience_prompt,
      skill SOP_EXTRACTION/AI_FEEDBACK renders, and dry_run _SOP_EVAL for fixed inputs
    - Store as expected snapshots the tests assert against
  </behavior>
  <action>
    Write snapshot tests using representative fixtures (a sample HcpProfile, Scenario,
    key_messages, transcript). Record the CURRENT (pre-migration) rendered strings as the
    golden snapshots. These pass now and must still pass after migration.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prompt_builder_registry.py -q</automated>
  </verify>
  <done>Snapshot tests capture current builder outputs and pass against the existing code.</done>
</task>

<task type="auto">
  <name>Task 2: Route builders through get_prompt</name>
  <files>backend/app/services/prompt_builder.py, backend/app/services/scoring_engine.py, backend/app/services/skill_conversion_service.py, backend/app/services/dry_run_engine.py, backend/app/services/conference_prompt_config.py</files>
  <action>
    Replace each hardcoded base template with `get_prompt(db, "<key>")` and render the
    resolved template using the same dynamic values via the existing render helpers
    (render_prompt_template / str.format-compatible {{placeholders}}). Keep function
    signatures stable; thread the AsyncSession where a builder previously had none by using
    the caller's session (or a resolver that accepts db). scoring.rubric keeps its per-entity
    behavior (rubric.prompt_template still wins when set); registry provides the base default.
    conference defaults now come from the registry key conference.audience / conference.moderator.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_prompt_builder_registry.py -q</automated>
  </verify>
  <done>All builders resolve via registry; snapshot tests still pass (output unchanged with defaults).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Override + full regression</name>
  <files>backend/tests/test_prompt_builder_registry.py</files>
  <behavior>
    - Test that activating a modified version for hcp.system changes build_hcp_system_prompt output
    - Test that scoring.rubric per-entity template still overrides the registry base
  </behavior>
  <action>
    Add override tests proving admin edits flow through, plus confirm the wider suite is green.
  </action>
  <verify>
    <automated>cd backend && pytest -q</automated>
  </verify>
  <done>Override tests pass; the full backend suite is green with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → builder render | Admin-controlled template text is rendered into an LLM system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Tampering | placeholder rendering | mitigate | Use the existing safe render helper (missing/extra placeholders do not crash; no eval) |
| T-27-09 | Repudiation | behavior change | mitigate | Snapshot regression guarantees default output is unchanged post-migration |
</threat_model>

<verification>
- Snapshot tests prove byte-identical output with seeded defaults.
- Override tests prove admin edits take effect.
- `pytest -q` full suite green.
</verification>

<success_criteria>
All builders resolve prompts from the registry; default output unchanged (snapshot-proven);
admin overrides take effect; full backend test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/27-prompt-optimizer-unified-prompt-management/27-03-SUMMARY.md`.
</output>
