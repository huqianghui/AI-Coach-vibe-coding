---
phase: 27-prompt-optimizer-unified-prompt-management
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - frontend/src/pages/admin/rubric-editor.tsx
  - frontend/src/components/admin/conference-audience-config.tsx
  - frontend/src/components/admin/prompt-optimize-dialog.tsx
  - backend/app/models/scenario.py
  - backend/alembic/env.py
  - infra/azure/prompt-optimizer.bicep
  - frontend/src/pages/admin/rubric-editor.test.tsx
  - frontend/e2e/prompt-management.spec.ts
autonomous: false
requirements: [PROMPT-05, PROMPT-01]
user_setup:
  - service: Azure Container Apps + Key Vault
    why: "Deploy the prompt-optimizer sidecar as an internal Container App with the Azure OpenAI key from Key Vault"
    dashboard_config:
      - task: "Store AZURE_OPENAI_API_KEY as a Key Vault secret and grant the app's managed identity get access"
        location: "Azure Portal -> Key Vault -> Secrets / Access policies"

must_haves:
  truths:
    - "The rubric editor exposes AI 优化 + version history for scoring.rubric prompts"
    - "The conference audience config exposes AI 优化 for conference.audience prompts"
    - "Both reuse the shared optimize dialog + management API (no duplicate logic)"
    - "The prompt-optimizer runs in Azure as an internal-ingress Container App with the key from Key Vault"
  artifacts:
    - path: "frontend/src/components/admin/prompt-optimize-dialog.tsx"
      provides: "Reusable optimize + diff + adopt dialog shared by all prompt editors"
      min_lines: 40
    - path: "infra/azure/prompt-optimizer.bicep"
      provides: "Internal Container App for the sidecar + Key Vault secret reference"
      contains: "prompt-optimizer"
  key_links:
    - from: "frontend/src/pages/admin/rubric-editor.tsx"
      to: "prompt-optimize-dialog"
      via: "shared dialog component"
      pattern: "PromptOptimizeDialog"
    - from: "infra/azure/prompt-optimizer.bicep"
      to: "Key Vault secret"
      via: "secretRef / managed identity"
      pattern: "keyVault|secretRef"
---

<objective>
Close the loop by bringing the two per-entity prompts (scoring.rubric, conference.audience)
into the same optimize + version experience via a shared dialog component, and deploy the
prompt-optimizer sidecar to Azure Container Apps with internal ingress and Key Vault-sourced
secrets.

Purpose: Uniform management across global AND per-entity prompts (F5), plus production-ready
Azure hosting of the optimizer (F1).
Output: shared optimize dialog, rubric/conference editor integration, scenario version field,
Azure Bicep for the internal sidecar, tests + E2E.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-prompt-optimizer-unified-prompt-management/27-RESEARCH.md
@frontend/src/pages/admin/rubric-editor.tsx
@frontend/src/components/admin/conference-audience-config.tsx
@frontend/src/hooks/use-prompts.ts
@backend/app/models/scenario.py
# Existing infra pattern for Container Apps
@infra/azure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared optimize dialog + per-entity integration</name>
  <files>frontend/src/components/admin/prompt-optimize-dialog.tsx, frontend/src/pages/admin/rubric-editor.tsx, frontend/src/components/admin/conference-audience-config.tsx, backend/app/models/scenario.py, backend/alembic/env.py</files>
  <action>
    Extract the optimize+diff+adopt UI from Plan 05's editor into a reusable
    PromptOptimizeDialog (props: initial content, mode default, onAdopt). Add an AI 优化 button
    wired to this dialog next to the prompt_template textarea in rubric-editor.tsx (reuses
    rubric.prompt_version for history) and next to audience_prompt_template in
    conference-audience-config.tsx. Add a `conference_prompt_version` int column to
    scenario.py + Alembic migration so conference prompts gain versioning parity.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npm run build</automated>
  </verify>
  <done>Both editors expose AI 优化 via the shared dialog; scenario gains a version column; frontend builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Azure internal sidecar (Bicep) + tests</name>
  <files>infra/azure/prompt-optimizer.bicep, frontend/src/pages/admin/rubric-editor.test.tsx, frontend/e2e/prompt-management.spec.ts</files>
  <action>
    Author prompt-optimizer.bicep: a Container App with INTERNAL ingress (VNet-only), the
    pinned upstream image, custom-provider env pointing at Azure OpenAI's /v1 endpoint (or the
    LiteLLM proxy chosen in Plan 02), and AZURE_OPENAI_API_KEY as a secretRef from Key Vault via
    managed identity. Backend references it by internal FQDN. Extend rubric-editor.test.tsx to
    assert the AI 优化 dialog opens and Adopt updates content; extend the E2E spec to cover
    optimizing a rubric prompt.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/admin/rubric-editor.test.tsx && npx playwright test --config=e2e/playwright.config.ts prompt-management</automated>
  </verify>
  <done>Bicep defines an internal-only sidecar with Key Vault secret; rubric optimize tests + E2E pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify internal Azure deployment + per-entity optimize</name>
  <action>Human verifies the Azure internal Container App, Key Vault secret, and a real rubric optimize round-trip; see how-to-verify steps below.</action>
  <what-built>prompt-optimizer deployed as an internal Azure Container App with Key Vault-sourced Azure OpenAI key; per-entity AI 优化 wired.</what-built>
  <how-to-verify>
    1. Deploy the Bicep (or review the plan in a what-if) and confirm the Container App has
       INTERNAL ingress (no public FQDN).
    2. From the backend Container App, confirm PROMPT_OPTIMIZER_MCP_URL resolves to the internal FQDN.
    3. In the admin UI, open a scoring rubric, click AI 优化, verify a real optimized result and Adopt.
    4. Confirm the Azure OpenAI key is a Key Vault secretRef, not a plaintext env value.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container App → Key Vault | Secret retrieval via managed identity |
| internal ingress | Sidecar reachable only within the environment/VNet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-16 | Info disclosure | sidecar in Azure | mitigate | Internal ingress only; no public FQDN |
| T-27-17 | Info disclosure | Azure OpenAI key | mitigate | Key Vault secretRef + managed identity; never in image or plaintext env |
| T-27-18 | License compliance | AGPL image in prod | mitigate | Deploy unmodified upstream image as a standalone service; no source linkage |
</threat_model>

<verification>
- `npx tsc -b` + `npm run build` pass; rubric optimize unit test + E2E pass.
- Checkpoint confirms internal-only Azure deployment with Key Vault secret and a real optimize round-trip.
</verification>

<success_criteria>
Per-entity prompts (rubric, conference) share the same optimize + version workflow via a
reusable dialog; the optimizer is deployed to Azure as an internal Container App with secrets
from Key Vault; tests + E2E pass and the deployment is human-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/27-prompt-optimizer-unified-prompt-management/27-06-SUMMARY.md`.
</output>
