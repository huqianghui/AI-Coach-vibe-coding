---
phase: 27-prompt-optimizer-unified-prompt-management
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - frontend/src/types/prompt.ts
  - frontend/src/api/prompts.ts
  - frontend/src/hooks/use-prompts.ts
  - frontend/src/pages/admin/prompts.tsx
  - frontend/src/pages/admin/prompt-editor.tsx
  - frontend/src/components/layouts/admin-layout.tsx
  - frontend/src/router/index.tsx
  - frontend/src/locales/zh/nav.json
  - frontend/src/locales/en/nav.json
  - frontend/src/locales/zh/prompts.json
  - frontend/src/locales/en/prompts.json
  - frontend/src/pages/admin/prompts.test.tsx
  - frontend/src/pages/admin/prompt-editor.test.tsx
  - frontend/e2e/prompt-management.spec.ts
autonomous: true
requirements: [PROMPT-05, PROMPT-04, PROMPT-03]

must_haves:
  truths:
    - "Admin sees a 'Prompt 管理' nav item and a list of all registered prompts"
    - "Admin can open a prompt, edit its content, and save a new version"
    - "Admin can click AI 优化, see an original-vs-optimized diff, and adopt it as a new version"
    - "Admin can view version history and roll back to any version"
    - "All UI text is i18n (zh + en); no inline useQuery in components"
  artifacts:
    - path: "frontend/src/pages/admin/prompts.tsx"
      provides: "Prompt list page (key, name, category, active version, last optimized)"
      min_lines: 30
    - path: "frontend/src/pages/admin/prompt-editor.tsx"
      provides: "Editor: content edit, placeholder chips, AI optimize + diff, version history + rollback"
      min_lines: 60
    - path: "frontend/src/hooks/use-prompts.ts"
      provides: "TanStack Query hooks per prompt-management operation"
      exports: ["usePrompts", "usePrompt", "useOptimizePrompt"]
    - path: "frontend/src/api/prompts.ts"
      provides: "Typed axios client for the prompt management API"
      exports: ["promptsApi"]
  key_links:
    - from: "frontend/src/pages/admin/prompt-editor.tsx"
      to: "use-prompts hooks"
      via: "hook calls (no inline useQuery)"
      pattern: "usePrompt|useOptimizePrompt"
    - from: "frontend/src/router/index.tsx"
      to: "prompts pages"
      via: "lazy route registration under /admin"
      pattern: "prompts"
    - from: "frontend/src/components/layouts/admin-layout.tsx"
      to: "/admin/prompts"
      via: "sidebar nav item"
      pattern: "/admin/prompts"
---

<objective>
Build the unified Prompt management UI in the admin app: a list page and an editor with
inline editing, placeholder hints, AI optimization (original-vs-optimized diff), adopt,
version history, and rollback. This is the F5 deliverable that makes the whole subsystem
usable by admins.

Purpose: One place to manage every prompt with full optimize + version workflow.
Output: types, API client, hooks, two pages, nav + route wiring, i18n, unit tests, E2E.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-prompt-optimizer-unified-prompt-management/27-RESEARCH.md
# Follow existing admin page + hook + router + nav patterns
@frontend/src/pages/admin/rubric-editor.tsx
@frontend/src/pages/admin/scoring-rubrics.tsx
@frontend/src/components/layouts/admin-layout.tsx
@frontend/src/router/index.tsx
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer (types, API client, hooks, i18n)</name>
  <files>frontend/src/types/prompt.ts, frontend/src/api/prompts.ts, frontend/src/hooks/use-prompts.ts, frontend/src/locales/zh/nav.json, frontend/src/locales/en/nav.json, frontend/src/locales/zh/prompts.json, frontend/src/locales/en/prompts.json</files>
  <action>
    Define TS types mirroring the backend schemas (Prompt, PromptVersion, PromptRun,
    OptimizeRequest/Response). Build promptsApi in api/prompts.ts using the shared axios
    client. Create use-prompts.ts TanStack Query hooks (usePrompts, usePrompt, usePromptVersions,
    usePromptRuns, useSaveVersion, useActivateVersion, useOptimizePrompt, useAdoptRun) — no
    inline useQuery in components. Add nav label `prompts` and a `prompts` i18n namespace (zh+en).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b</automated>
  </verify>
  <done>Types compile; hooks + API client + i18n keys exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: List + editor pages, nav + route</name>
  <files>frontend/src/pages/admin/prompts.tsx, frontend/src/pages/admin/prompt-editor.tsx, frontend/src/components/layouts/admin-layout.tsx, frontend/src/router/index.tsx</files>
  <action>
    Build the list page (table: key, name, category, active version, last optimized). Build the
    editor page: content textarea, placeholder chips (from variables), Save (new version),
    version-history list with Rollback (activate), and an AI 优化 button opening a dialog with
    mode select (system/user/iterate; iterate shows a requirements input), calling useOptimizePrompt,
    showing original-vs-optimized diff, with Adopt. Add `{ path: "/admin/prompts", labelKey: "prompts",
    icon: FileText }` to the `content` sidebar group and register lazy routes `prompts` and
    `prompts/:key` under /admin in router/index.tsx (cn() for conditional classes).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Nav item + routes render; editor supports edit/save/optimize/diff/adopt/rollback; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests + Playwright E2E</name>
  <files>frontend/src/pages/admin/prompts.test.tsx, frontend/src/pages/admin/prompt-editor.test.tsx, frontend/e2e/prompt-management.spec.ts</files>
  <behavior>
    - vitest: list renders rows; editor Save calls useSaveVersion; AI 优化 opens diff and Adopt calls useAdoptRun; Rollback calls useActivateVersion (mock hooks/API)
    - E2E user story: admin logs in -> opens Prompt 管理 -> opens a prompt -> clicks AI 优化 -> sees diff -> adopts -> new active version shown -> rolls back -> prior version active
  </behavior>
  <action>
    Write vitest component tests mocking the hooks/API. Write a Playwright spec covering the
    core admin user story end to end (mock or seeded backend). Run with the project e2e config.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/admin/prompts.test.tsx src/pages/admin/prompt-editor.test.tsx && npx playwright test --config=e2e/playwright.config.ts prompt-management</automated>
  </verify>
  <done>Component tests and the E2E user story pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin routes | Admin-only pages must be guarded |
| rendered diff/content | Prompt content rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14 | Elevation of privilege | /admin/prompts | mitigate | Routes live under the existing AdminRoute guard |
| T-27-15 | Info disclosure (XSS) | diff/content render | mitigate | Render prompt text as plain text (React escaping); do not use dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npx tsc -b` and `npm run build` pass.
- vitest component tests pass.
- Playwright E2E core user story passes.
</verification>

<success_criteria>
Admins manage all prompts from a single UI: browse, edit→version, AI-optimize→diff→adopt,
and rollback — fully i18n, guarded, with unit + E2E coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/27-prompt-optimizer-unified-prompt-management/27-05-SUMMARY.md`.
</output>
