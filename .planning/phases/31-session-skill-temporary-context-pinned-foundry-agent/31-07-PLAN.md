---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 07
type: execute
wave: 7
depends_on: [31-06]
files_modified:
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md
autonomous: true
requirements: [R2-TEXT, R2-WS, R2-WEBRTC, R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "Every backend/frontend/E2E/live gate passes before release"
    - "100% changed-code coverage is proven for Requirement 2 code"
    - "Protected paths and Phase 30 evidence are byte-identical and unstaged"
    - "Exactly one allowlisted Requirement 2 commit is pushed once and remote SHA equals local SHA"
  artifacts:
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md
      provides: Complete nonsecret gate, live, coverage, hash, staging, commit, and push evidence
  key_links:
    - from: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md
      to: origin/feat/0616_shuning
      via: one allowlisted commit after all gates
      pattern: "remote SHA.*local SHA"
---
<objective>Run all final gates and live acceptance, validate protected work, then create the single Requirement 2 commit and push.</objective>
<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-CONTINUE.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Establish immutable release and protected-path manifests</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md</files>
  <action>Record branch, pre-release HEAD, origin SHA, and `git status --short`. Hash every `.planning/debug/*`, every `backend/storage/db-backups/*`, tracked/untracked database file/sidecar, and Phase 30 `30-ACCEPTANCE.md`, `30-06-SUMMARY.md`, `30-CONTINUE.md`; record byte size/SHA-256. Also record the explicit expected Phase 31 allowlist from actual changed production/test/planning paths. Do not modify protected files or clean/reset/stash.</action>
  <verify><automated>$p='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md'; if (-not (Test-Path $p)) { exit 1 }; if ((Get-Content $p -Raw) -notmatch 'Pre-release HEAD' -or (Get-Content $p -Raw) -notmatch 'Protected SHA-256 manifest') { exit 1 }</automated></verify>
  <done>Release baseline, protected hashes, and explicit staging allowlist are recorded before final tests.</done>
</task>
<task type="auto">
  <name>Task 2: Run all offline, coverage, E2E, protected-hash, and live gates</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md</files>
  <action>Run strictly in order and stop on any failure: (1) backend Ruff check and format check over the full backend; (2) full non-integration pytest with coverage XML and configured 89% floor; (3) `diff-cover coverage.xml --compare-branch=<pre-release-head> --fail-under=100` for Requirement 2 changed Python; (4) full frontend `npm run test:coverage` with configured thresholds and no waiver; (5) `diff-cover coverage/lcov.info --compare-branch=<pre-release-head> --fail-under=100` for changed executable TS/TSX, with type-only changes proven by `tsc`; (6) `npx tsc -b`; (7) `npm run build`; (8) full Playwright suite using configured browser, plus explicit Requirement 2 spec nonzero pass/zero fail/skip; (9) rerun Phase 31 live probes with opt-in and require zero skip/fail, exact Dr-Chen-Jun v5, context+IQ evidence, and the approved WebRTC verdict; (10) rerun existing Phase 30 IQ acceptance to prove exact native `knowledge_base_retrieve` non-regression; (11) `git diff --check`, scope sweep for forbidden fallback/Agent-write/browser-context patterns, and protected hash equality. Record exact commands/counts/coverage/nonsecret IDs. No gate waiver is allowed for Requirement 2.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\ruff.exe check .; .\.venv\Scripts\ruff.exe format --check .; .\.venv\Scripts\python.exe -m pytest -v --cov=app --cov-report=term-missing --cov-report=xml; diff-cover coverage.xml --compare-branch=HEAD --fail-under=100; Set-Location ..\frontend; npm run test:coverage; npx tsc -b; npm run build; npx playwright test --config=e2e/playwright.config.ts --project=chromium; Set-Location ..\backend; $env:PHASE31_LIVE_PROBES='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py tests/integration/test_unified_training_foundry_kb.py -m integration -vv -rs</automated></verify>
  <done>Acceptance records every required green gate, 100% changed-code coverage, zero-skipped live proof, and exact protected hash equality.</done>
</task>
<task type="auto">
  <name>Task 3: Explicit allowlist staging, one commit, and one push</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.md</files>
  <action>Only after Task 2 is fully green, stage each expected Phase 31 file by explicit path. Never use `git add .` or `git add -A`. Audit `git diff --cached --name-only` against the recorded allowlist; fail if it includes protected paths, DB/coverage/log artifacts, or Phase 30 acceptance/summary files. Inspect cached diff and run `git diff --cached --check`. Create exactly one commit `feat: apply Session Skill context to pinned Foundry Agent`; verify `git rev-list --count <pre-release-head>..HEAD` equals 1. Push exactly once with `git push origin HEAD:feat/0616_shuning`, then verify remote SHA equals local HEAD and protected hashes remain equal. If auth/network prevents push, stop without a second commit or alternative branch.</action>
  <verify><automated>$branch=git branch --show-current; if ($branch -ne 'feat/0616_shuning') { exit 1 }; $local=git rev-parse HEAD; $remote=git rev-parse origin/feat/0616_shuning; if ($local -ne $remote) { exit 1 }; $bad=git diff --cached --name-only | Select-String '(^\.planning/debug/|^backend/storage/db-backups/|\.db(-shm|-wal)?$|30-ACCEPTANCE\.md$|30-06-SUMMARY\.md$)'; if ($bad) { exit 1 }</automated></verify>
  <done>Exactly one Requirement 2 commit exists, was pushed once, remote equals local, and only explicit allowlisted files were staged.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Working tree → commit | Explicit file allowlist, cached-name/diff audit |
| Local → remote | One push, SHA equality |
| Live environment → release claim | Zero skip/fail and sanitized evidence |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-07-01 | Tampering | Git staging | mitigate | Never broad-add; explicit allowlist and cached audit |
| T-31-07-02 | Repudiation | Acceptance | mitigate | Exact commands/counts/SHAs/evidence recorded |
| T-31-07-03 | Information Disclosure | Logs/evidence | mitigate | Nonsecret IDs/digests only; exclude logs from staging |
| T-31-07-04 | Tampering | Protected work | mitigate | Pre/post SHA-256 equality and staging exclusion |
</threat_model>
<verification>No commit/push occurs until every gate is green. Final acceptance proves one commit, one push, remote equality, and protected-path integrity.</verification>
<success_criteria>Requirement 2 is fully tested/live-proven, safely released as one isolated commit, with no protected or Phase 30 evidence changes.</success_criteria>
<output>Create `31-07-SUMMARY.md` after release. If generated after the sole commit, leave it uncommitted and report that rather than amending or creating a second commit.</output>
