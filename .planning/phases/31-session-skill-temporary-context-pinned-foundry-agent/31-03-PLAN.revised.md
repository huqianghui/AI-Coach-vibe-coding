---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 03
type: tdd
wave: 3
depends_on: [31-02R]
files_modified:
  - backend/app/models/session.py
  - backend/app/models/session_turn_context_audit.py
  - backend/app/models/__init__.py
  - backend/app/schemas/session.py
  - backend/alembic/versions/b35a_phase31_session_context_conversations.py
  - backend/tests/test_phase31_session_context_models.py
  - backend/tests/test_phase31_migration_roundtrip.py
autonomous: true
requirements: [R2-SOP, R2-TEXT, R2-TRUST]
must_haves:
  truths:
    - "Every newly created Skill-bound Session can persist an immutable structured SOP snapshot and digest"
    - "A Session can persist one server-owned Foundry Conversation lifecycle without exposing it in public schemas"
    - "Every terminal turn has one immutable append-only audit row with an idempotency key and exact authority/correlation data"
    - "Historical Skill-bound Sessions without a valid snapshot remain representable but fail closed in later services"
  artifacts:
    - path: backend/app/models/session_turn_context_audit.py
      provides: Append-only per-turn context audit model
    - path: backend/alembic/versions/b35a_phase31_session_context_conversations.py
      provides: Reversible schema for snapshot, Conversation lifecycle, and audit
  key_links:
    - from: backend/app/models/session_turn_context_audit.py
      to: backend/app/models/session.py
      via: RESTRICT foreign key retained at least as long as parent Session
      pattern: "ondelete=\"RESTRICT\""
---

<objective>
Create the irreversible persistence foundation for immutable SOP authority, server-owned Foundry Conversation lifecycle, and append-only per-turn audit.

Purpose: Make behavior, continuation, retries, cleanup, and progression durable and auditable before any production request path is changed.
Output: ORM/schema exports, one Alembic migration, and 100%-covered model plus upgrade/downgrade tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PLANNING-STATUS.revised.md
@backend/app/models/session.py
@backend/app/models/message.py
@backend/app/models/__init__.py
@backend/alembic/versions/a34a_add_session_agent_pin.py

<decision_coverage>
D-05: Task 1 full; D-06: Tasks 1-2 full persistence; D-12: Tasks 1-2 full; D-17: all tasks full. No Voice/avatar/WebRTC implementation.
</decision_coverage>
</context>

<feature>
  <name>Phase 31 Session context persistence</name>
  <files>backend/app/models/session.py, backend/app/models/session_turn_context_audit.py, backend/app/models/__init__.py, backend/app/schemas/session.py, backend/alembic/versions/b35a_phase31_session_context_conversations.py, backend/tests/test_phase31_session_context_models.py, backend/tests/test_phase31_migration_roundtrip.py</files>
  <behavior>
    - Add nullable `sop_snapshot_json` and `sop_snapshot_sha256`; no guessed backfill. JSON schema contains schema_version, skill_id, skill_version_id, skill_name, ordered immutable steps, and source-content digest.
    - Add internal-only Conversation fields: ID, state (`unprovisioned|provisioning|active|cleanup_pending|closed|failed`), creation/cleanup timestamps, bounded retry count, sanitized last error, lease token/expiry, and monotonically increasing context revision.
    - Add `SessionTurnContextAudit` with unique `(session_id,idempotency_key)`, transport, exact Agent pin, Skill/version IDs, snapshot/focus/applied-context digests, context revision, applied step, user/assistant message IDs, Foundry response ID, Conversation ID digest (not raw public exposure), progression outcome/next step, terminal status, sanitized MCP call correlation metadata, and created timestamp.
    - Audit FK uses RESTRICT and no ORM delete cascade; ORM/service guards reject update/delete so rows are append-only.
    - Public `SessionResponse` does not expose Foundry Conversation ID, developer context, lease, or continuation fields.
    - Migration upgrade works on populated SQLite and PostgreSQL-compatible DDL; downgrade removes all additions and returns to the previous head.
  </behavior>
  <implementation>Write failing metadata, uniqueness, append-only, public-schema exclusion, and migration roundtrip tests first. Then add the model and a single migration chained from the actual current Alembic head discovered at execution. Use batch operations where SQLite requires them. Keep snapshot columns nullable solely for historical compatibility; later resolution must reject missing/invalid values. Do not modify a DB file or delete/recreate databases. Export the audit model through `app.models` and ensure Alembic imports it. Document RESTRICT retention semantics in model docstrings.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define immutable snapshot, lifecycle, and audit contracts</name>
  <files>backend/app/models/session.py, backend/app/models/session_turn_context_audit.py, backend/app/models/__init__.py, backend/app/schemas/session.py, backend/tests/test_phase31_session_context_models.py</files>
  <action>Implement the feature's ORM contracts after failing tests. Use `Text` for canonical JSON, SHA-256 hex strings for digests, explicit indexes for cleanup state and Session audit ordering, and uniqueness for idempotency. Add SQLAlchemy event guards for update/delete of persisted audit rows. Keep all Conversation authority internal; no request or response schema may accept/expose it.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_session_context_models.py -vv --cov=app.models.session --cov=app.models.session_turn_context_audit --cov-report=term-missing --cov-fail-under=100</automated></verify>
  <done>Model tests prove field contracts, digest lengths, uniqueness, append-only behavior, retention FK, exports, and browser-schema exclusion at 100% coverage.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add and roundtrip the Phase 31 migration</name>
  <files>backend/alembic/versions/b35a_phase31_session_context_conversations.py, backend/tests/test_phase31_migration_roundtrip.py</files>
  <action>Create one hand-reviewed/autogenerated migration from the actual single head. Test upgrade on a populated pre-Phase-31 database, insert/read a complete snapshot/lifecycle/audit roundtrip, prove unique and RESTRICT constraints, downgrade one revision, and upgrade again. Never backfill structured SOP from mutable current Skills and never touch workspace DB files.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_migration_roundtrip.py -vv; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\ruff.exe check app/models/session.py app/models/session_turn_context_audit.py app/models/__init__.py app/schemas/session.py alembic/versions/b35a_phase31_session_context_conversations.py tests/test_phase31_session_context_models.py tests/test_phase31_migration_roundtrip.py; .\.venv\Scripts\ruff.exe format --check app/models/session.py app/models/session_turn_context_audit.py app/models/__init__.py app/schemas/session.py alembic/versions/b35a_phase31_session_context_conversations.py tests/test_phase31_session_context_models.py tests/test_phase31_migration_roundtrip.py</automated></verify>
  <done>Exactly one head exists and migration upgrade/downgrade plus populated roundtrip passes without a guessed historical backfill.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Mutable Skill tables → Session | Immutable canonical snapshot and digest at creation only |
| ORM callers → audit | Append-only ORM guards plus DB uniqueness/RESTRICT |
| Server lifecycle → browser schema | Internal fields excluded from input/output |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-03-01 | Tampering | SOP snapshot | mitigate | Canonical JSON and SHA-256 digest |
| T-31-03-02 | Repudiation | Turn context | mitigate | Unique immutable audit row with exact pin and correlations |
| T-31-03-03 | Information Disclosure | Conversation ID/context | mitigate | Internal ORM only; schema exclusion tests |
| T-31-03-04 | Denial of Service | Orphan cleanup rows | mitigate | Indexed lifecycle state and bounded retry fields |
</threat_model>

<verification>Both focused suites pass, changed executable Python is 100% covered, Ruff passes, migration has one head and roundtrips.</verification>
<success_criteria>The database can durably represent all Requirement 2 authority without exposing it or fabricating historical snapshots.</success_criteria>
<output>Create `31-03-SUMMARY.revised.md`; do not commit or push.</output>
