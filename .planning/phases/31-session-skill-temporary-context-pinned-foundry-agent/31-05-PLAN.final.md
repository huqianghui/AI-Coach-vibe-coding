---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 05
type: tdd
wave: 3
depends_on: [31-04-PLAN.final.md]
files_modified:
  - backend/app/services/foundry_conversation_service.py
  - backend/app/services/session_turn_orchestrator.py
  - backend/app/services/session_conversation_cleanup.py
  - backend/app/services/agent_chat_service.py
  - backend/app/services/session_service.py
  - backend/app/main.py
  - backend/tests/test_foundry_conversation_service.py
  - backend/tests/test_session_turn_orchestrator.py
  - backend/tests/test_session_conversation_cleanup.py
  - backend/tests/test_agent_chat_service.py
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-CHANGED-PYTHON-SOURCE.txt
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-CHANGED-PYTHON-TESTS.txt
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-START-SNAPSHOT.json
autonomous: true
requirements: [R2-TEXT, R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "One active Session maps to one durable server-owned Foundry Conversation without blind replacement"
    - "Every provider invocation has an immutable attempt and only one response can commit as winner"
    - "Unknown outcomes reconcile or fail closed; the system never claims provider exactly-once"
    - "End/delete/expiry and startup-periodic cleanup durably close Conversations"
  artifacts:
    - path: backend/app/services/foundry_conversation_service.py
      provides: Honest Conversation and provider contract
    - path: backend/app/services/session_turn_orchestrator.py
      provides: Transactional outbox/attempt/winner orchestration
    - path: backend/app/services/session_conversation_cleanup.py
      provides: Startup and periodic bounded cleanup
  key_links:
    - from: backend/app/services/session_turn_orchestrator.py
      to: backend/app/services/foundry_conversation_service.py
      via: developer item then user item then exact-agent Response on explicit Conversation
      pattern: "developer"
---

<objective>Implement the proven Foundry Conversation text path with durable leases, retries, immutable attempts, winner semantics, reconciliation, cleanup, and exact Session authority.</objective>

<execution_context>@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-EXECUTION-STATUS.final.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-RESEARCH-STATUS.final.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-VALIDATION.final.md</execution_context>

<context>@backend/app/services/agent_chat_service.py
@backend/app/services/session_service.py
@backend/app/main.py
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md</context>

<preflight>Before any Plan 05 edit, from repository root run `$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; .\backend\.venv\Scripts\python.exe scripts\verify_python_changed_branches.py snapshot --baseline $env:PHASE31_BASELINE_SHA --output .planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-05-START-SNAPSHOT.json`. It refuses overwrite; membership is computed against this snapshot.</preflight>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement honest Conversation create, reuse, unknown-create, and cleanup lifecycle</name>
  <files>backend/app/services/foundry_conversation_service.py, backend/app/services/session_conversation_cleanup.py, backend/app/services/session_service.py, backend/app/main.py, backend/tests/test_foundry_conversation_service.py, backend/tests/test_session_conversation_cleanup.py</files>
  <behavior>
    - First text turn transactionally claims a time-bounded create lease; after commit it calls provider create; a known ID is persisted in a second transaction before items/responses begin.
    - If provider supports idempotency key/operation metadata, persist it before call and reuse it for reconciliation. Tests introspect the installed client/service contract; unsupported metadata is recorded as unsupported, never invented.
    - Timeout/disconnect after create becomes `create_unknown`. Reconciliation retrieves by operation/idempotency metadata only if proven supported. Without that support, block new turns and quarantine until retention/operator cleanup; never blind-create a replacement and never claim exactly-once.
    - Active mapping is always reused. A provider 404 for a previously persisted mapping becomes terminal lifecycle failure/cleanup evidence, not replacement creation.
    - End, explicit delete request, expiry, and retention sweep atomically block new turns, mark cleanup pending, then request provider deletion. Confirmed absent/deleted becomes closed; transient/unknown deletion schedules bounded exponential retry with jitter ceiling and sanitized errors.
    - Startup runs one bounded sweep. A single in-process periodic task runs at configured interval, uses DB leases so multiple app instances do not duplicate active cleanup, stops on lifespan shutdown, and never prevents startup. Tests use controllable clock—no sleeps.
    - Parent Session is retained because attempts/audits use RESTRICT; cleanup precedes any permitted administrative purge.
  </behavior>
  <action>Wrap blocking SDK operations with `asyncio.to_thread`, explicit connect/call deadlines, and cancellation-safe lease release. Transaction boundaries must be visible in tests. Add startup and periodic cleanup wiring to lifespan without changing unrelated startup paths. Validate retention and multi-worker lease behavior.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_foundry_conversation_service.py tests/test_session_conversation_cleanup.py -vv --cov=app.services.foundry_conversation_service --cov=app.services.session_conversation_cleanup --cov-branch --cov-report=term-missing --cov-fail-under=100</automated></verify>
  <done>Create/persist/reuse/unknown-create/end/delete/expiry/startup/periodic/retry/retention/RESTRICT paths and metadata limitations are honestly covered at 100%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate outbox, attempts, retries, winner, duplicates, and exact Agent response</name>
  <files>backend/app/services/session_turn_orchestrator.py, backend/app/services/agent_chat_service.py, backend/tests/test_session_turn_orchestrator.py, backend/tests/test_agent_chat_service.py</files>
  <behavior>
    - Claim transaction reuses the one unfinished server turn or creates a new pending turn, freezes context, acquires lease, commits, then performs external calls outside DB transactions.
    - Each invocation appends and commits a new immutable attempt start fact before external call. Every dispatch, timeout, success, failure, reconciliation, winner, and late-duplicate fact is appended as a separate immutable `SessionTurnAttemptEvent`; neither attempt nor event rows are updated. IDs learned after dispatch exist only in event rows and the mutable outbox aggregate.
    - Append exactly one final developer item immediately before one user item. Create Response on explicit Conversation using exact Session `agent_reference`. Prohibit top-level instructions, Response-input developer/system, `previous_response_id`, tools/tool_choice, Agent writes, HCP-latest, generic adapter, and model/transport fallback.
    - On known success, a single transaction verifies lease/status, appends `known_success` and `winner_selected` events, writes assistant message and immutable context audit, marks the mutable outbox succeeded/winning attempt, and commits monotonic progression. Unique/CAS conflict appends a `late_duplicate` event in a new transaction and never writes a second assistant/audit/progression result.
    - Timeout/disconnect after request becomes provider_unknown. Reconcile by proven provider operation/response ID; known terminal success can compete for winner, known failure can retry within budget, unknown without lookup remains fail closed. Never blindly resend an unknown operation.
    - Duplicate stream terminal/MCP events are deduplicated. Failed/uncorrelated/other-name IQ events never count. Exact successful `knowledge_base_retrieve` correlation is saved to audit.
    - Cancellation releases/lets lease expire without discarding pending authority. Retry responses expose `in_progress`, `reconciling`, committed winner replay, or terminal failure semantics without provider duplication claims.
    - Standalone `chat_with_agent()` and Phase 30 non-Skill paths keep their existing continuation behavior.
  </behavior>
  <action>Implement a typed Session-only API and explicit transaction helpers. Tests must simulate crash points after every commit/external boundary, two workers, timeout then late success, timeout without provider lookup, duplicate terminals, loser response, lease expiry, retry budget, rollback, and immutable attempt append. Add behavioral fake-provider tests where stale focus conflicts with the final directive and output follows the final current step.</action>
  <verify><automated>$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_turn_orchestrator.py tests/test_agent_chat_service.py -vv --cov=app.services.session_turn_orchestrator --cov=app.services.agent_chat_service --cov-branch --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/session_turn_orchestrator.py,app/services/foundry_conversation_service.py,app/services/session_conversation_cleanup.py'; .\.venv\Scripts\ruff.exe check app/services/foundry_conversation_service.py app/services/session_turn_orchestrator.py app/services/session_conversation_cleanup.py app/services/agent_chat_service.py app/services/session_service.py app/main.py tests/test_foundry_conversation_service.py tests/test_session_turn_orchestrator.py tests/test_session_conversation_cleanup.py tests/test_agent_chat_service.py</automated></verify>
  <done>Crash/retry/reconcile/winner/late-duplicate/behavior/IQ/standalone cases pass with at-most-one committed result and 100% changed statements/branches.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce Plan 05 changed Python statements and branches</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-CHANGED-PYTHON-SOURCE.txt, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-CHANGED-PYTHON-TESTS.txt, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-05-START-SNAPSHOT.json</files>
  <action>Classify every changed Python path exactly once into source or tests. Run all tests with branch JSON and JUnit, then use the exact verifier CLI against the immutable snapshot. Any classification, execution, coverage, or snapshot failure blocks Plan 06.</action>
  <verify><automated>$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_foundry_conversation_service.py tests/test_session_turn_orchestrator.py tests/test_session_conversation_cleanup.py tests/test_agent_chat_service.py -vv --junitxml=phase31-plan05-junit.xml --cov=app.services.foundry_conversation_service --cov=app.services.session_turn_orchestrator --cov=app.services.session_conversation_cleanup --cov=app.services.agent_chat_service --cov=app.services.session_service --cov=app.main --cov-branch --cov-report=json:phase31-plan05-coverage.json; .\.venv\Scripts\python.exe ..\scripts\verify_python_changed_branches.py verify --coverage-json phase31-plan05-coverage.json --source-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-05-CHANGED-PYTHON-SOURCE.txt --test-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-05-CHANGED-PYTHON-TESTS.txt --start-snapshot ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-05-START-SNAPSHOT.json --junit phase31-plan05-junit.xml</automated></verify>
  <done>Every Plan 05 changed executable Python statement and branch is machine-verified at 100%, with complete manifest membership.</done>
</task>
</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Duplicate provider side effect | mitigate | idempotency where proven; unknown quarantine/reconciliation; no blind resend |
| Two committed results | mitigate | lease + unique/CAS winner transaction |
| Stale context wins | mitigate | adjacent final developer directive and behavioral tests |
| Orphan Conversation | mitigate | durable cleanup pending, startup/periodic leased sweeps |
</threat_model>

<verification>Focused lifecycle/orchestrator/regression tests and Ruff pass with 100% changed statement/branch coverage.</verification>
<success_criteria>Production text has durable honest orchestration, one committed winner, immutable evidence, and complete Conversation lifecycle.</success_criteria>
<output>Create a new `31-05-SUMMARY.final.md`; do not commit or push.</output>
