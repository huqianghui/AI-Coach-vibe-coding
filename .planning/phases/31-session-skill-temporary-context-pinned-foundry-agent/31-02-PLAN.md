---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - backend/app/services/session_skill_context.py
  - backend/app/services/skill_focus_service.py
  - backend/tests/test_session_skill_context.py
  - backend/tests/test_skill_focus_service.py
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md
autonomous: true
requirements: [R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "One immutable validated envelope renders the same bounded server context for every transport"
    - "Existing focus_instruction snapshot content is read unchanged and never refreshed from latest Skill state"
    - "Progress detection has typed non-heuristic outcomes and cannot regress the committed step"
    - "A written evidence-based decision determines whether durable audit needs a schema change"
  artifacts:
    - path: backend/app/services/session_skill_context.py
      provides: Canonical envelope, renderer, digest, validation, and audit metadata
      exports: [SessionSkillContextEnvelope, build_session_skill_context, render_session_skill_context]
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md
      provides: Migration/no-migration audit decision and reconstruction proof
  key_links:
    - from: backend/app/services/session_skill_context.py
      to: backend/app/models/session.py
      via: reads exact pin, Skill snapshot, focus snapshot, and committed SOP step
      pattern: "agent_name|agent_version|focus_instruction|sop_current_step"
---

<objective>Create the canonical immutable context contract and make the durable-audit decision without presuming a migration.

Purpose: All transports need one trustworthy rendering and one explicit progression/audit policy before integration.
Output: Tested core service, typed detector result, and written schema decision.</objective>

<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@backend/app/models/session.py
@backend/app/models/message.py
@backend/app/services/skill_focus_service.py
@backend/app/services/session_service.py

<interfaces>
`CoachingSession` already owns `agent_name`, `agent_version`, `skill_id`, `skill_version_id`, `focus_instruction`, and `sop_current_step`. Preserve their semantics per D-05. `resolve_pinned_agent()` remains the exact identity validator.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and test the immutable context envelope</name>
  <files>backend/app/services/session_skill_context.py, backend/tests/test_session_skill_context.py</files>
  <behavior>
    - Build only from an ownership-resolved `CoachingSession`; reject missing pin, Skill/version snapshot, empty focus snapshot, invalid step, or oversized rendering before a transport call.
    - Frozen envelope includes schema version, session/Skill/version IDs, exact Agent name/version, unchanged focus snapshot, committed step, turn key/number, deterministic SHA-256 digest, and redacted audit metadata.
    - Renderer delimits trusted training objective, preserves HCP persona/tool authority, forbids disclosure, and never includes credentials/tool schemas/browser fields.
    - `repr`/logs do not reveal raw focus content.
  </behavior>
  <action>Write tests first, then implement frozen/slotted dataclasses or immutable Pydantic contract plus `build_session_skill_context()` and `render_session_skill_context()`. Use a deterministic maximum and fail closed instead of truncating. Do not load Skill content, mutate Session fields, use the word `latest` as a version substitute, or add browser schemas (D-01–D-05).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_skill_context.py -vv --cov=app.services.session_skill_context --cov-report=term-missing --cov-fail-under=100; .\.venv\Scripts\ruff.exe check app/services/session_skill_context.py tests/test_session_skill_context.py; .\.venv\Scripts\ruff.exe format --check app/services/session_skill_context.py tests/test_session_skill_context.py</automated></verify>
  <done>Envelope validation/rendering/digest/redaction has 100% module coverage and cannot mutate or refresh Session snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace heuristic detector semantics with typed outcomes</name>
  <files>backend/app/services/skill_focus_service.py, backend/tests/test_skill_focus_service.py</files>
  <behavior>
    - Detector returns `advanced`, `unchanged`, `indeterminate`, or `failed` with validated step/evidence.
    - Provider/config/parse failures never return an artificial successful step.
    - Result cannot regress below committed step or jump outside SOP bounds.
  </behavior>
  <action>Add an immutable `SopProgressDecision`/enum contract and refactor `detect_sop_step()` to return it. Keep SOP extraction and focus snapshot composition backward-compatible; do not rewrite existing snapshot semantics. Remove the error-to-zero and no-endpoint heuristic-success meaning, but do not wire progression yet. Update all directly affected existing tests.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_skill_focus_service.py -vv --cov=app.services.skill_focus_service --cov-report=term-missing --cov-fail-under=100; .\.venv\Scripts\ruff.exe check app/services/skill_focus_service.py tests/test_skill_focus_service.py; .\.venv\Scripts\ruff.exe format --check app/services/skill_focus_service.py tests/test_skill_focus_service.py</automated></verify>
  <done>Every detector outcome and failure branch is tested; no failure/absence can silently advance SOP.</done>
</task>

<task type="auto">
  <name>Task 3: Decide durable audit persistence from reconstruction evidence</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md</files>
  <action>Trace existing durable Session/message fields and the proposed turn metadata. Document required audit questions: exact pin, Skill/version/focus snapshot, context digest/schema, committed/applied step, response ID/transport, progression outcome, timestamp/idempotency. Choose **NO MIGRATION** only if these can be durably reconstructed without relying on ephemeral logs; otherwise choose **APPEND-ONLY AUDIT TABLE REQUIRED**, specify exact columns/retention/uniqueness, and stop execution so Plans 31-03–31-07 are revised to include ORM/Alembic/tests before integration. Do not create migration/model files in this plan (D-11).</action>
  <verify><automated>$p='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md'; if (-not (Test-Path $p)) { exit 1 }; $c=Get-Content $p -Raw; if ($c -notmatch 'Decision: (NO MIGRATION|APPEND-ONLY AUDIT TABLE REQUIRED)') { exit 1 }; if ($c -notmatch 'Reconstruction evidence') { exit 1 }</automated></verify>
  <done>Audit decision is explicit and evidenced; if a table is required, later plans are blocked pending revision rather than silently assuming a migration.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Session DB → context | Strict required fields/bounds/digest; no mutable lookup |
| Skill snapshot → Agent | Delimited content, identity/tool authority restated, redacted logs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-02-01 | Tampering | Envelope | mitigate | Frozen value and deterministic digest |
| T-31-02-02 | Information Disclosure | Logs/repr | mitigate | IDs/digest/length only |
| T-31-02-03 | Repudiation | Turn audit | mitigate | Evidence-based durable reconstruction decision |
| T-31-02-04 | Tampering | SOP progression | mitigate | Typed outcomes, monotonic validation, no heuristic success |
</threat_model>
<verification>Both focused suites reach 100% module coverage; Ruff passes; audit decision is complete. No later plan proceeds if revision is required.</verification>
<success_criteria>A transport-independent trusted context exists and audit/schema scope is resolved without research overreach.</success_criteria>
<output>Create `31-02-SUMMARY.md`; do not commit/push.</output>
