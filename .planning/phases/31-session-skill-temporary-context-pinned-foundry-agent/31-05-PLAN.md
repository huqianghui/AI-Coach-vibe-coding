---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 05
type: execute
wave: 5
depends_on: [31-04]
files_modified:
  - backend/app/services/voice_live_webrtc.py
  - backend/app/api/voice_live.py
  - backend/app/schemas/voice_live.py
  - backend/tests/test_voice_live_webrtc.py
  - frontend/src/api/voice-live.ts
  - frontend/src/types/voice-live.ts
  - frontend/src/hooks/use-voice-live-webrtc.ts
  - frontend/src/api/voice-live.test.ts
  - frontend/src/hooks/use-voice-live-webrtc.test.ts
autonomous: true
requirements: [R2-WEBRTC, R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "The implementation follows the approved live verdict: proven server authority or explicit Session-bound fail-closed"
    - "The browser never receives Azure credentials/control URL or context on the proven server-owned path"
    - "No client Session-bound response.create, session.update, identity, tools, or instructions remain"
    - "Unsupported WebRTC never falls back to WS/model/generic/latest behavior"
  artifacts:
    - path: backend/app/services/voice_live_webrtc.py
      provides: Proven server control/signaling path or structured unsupported gate
    - path: frontend/src/hooks/use-voice-live-webrtc.ts
      provides: Session-safe WebRTC browser contract or deterministic unsupported handling
  key_links:
    - from: backend/app/services/voice_live_webrtc.py
      to: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
      via: implementation branch selected only by approved verdict
      pattern: "PROVEN|FAIL-CLOSED"
---
<objective>Implement only the WebRTC topology proven safe by Plan 31-01, otherwise fail Session-bound WebRTC closed.</objective>
<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@backend/app/services/voice_live_webrtc.py
@backend/app/api/voice_live.py
@frontend/src/hooks/use-voice-live-webrtc.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the approved backend WebRTC verdict</name>
  <files>backend/app/services/voice_live_webrtc.py, backend/app/api/voice_live.py, backend/app/schemas/voice_live.py, backend/tests/test_voice_live_webrtc.py</files>
  <behavior>
    - PROVEN: backend authenticates, retains control WS, performs SDP/session config, disables auto-response, creates each response with canonical context, correlates transcripts, and does not return auth/control/context.
    - PROVEN: browser bypass attempt is prevented by the exact mechanism demonstrated in capability evidence.
    - FAIL-CLOSED: owned `session_id` returns structured `SESSION_WEBRTC_CONTEXT_UNSUPPORTED` before token exchange/signaling; standalone admin/test WebRTC remains separate.
    - Both branches preserve exact Session pin and prohibit fallback.
  </behavior>
  <action>Read the approved verdict. If PROVEN, implement the demonstrated sideband authority and expose only a backend-mediated SDP/status contract; include lifecycle cleanup/timeouts and shared progression. If FAIL-CLOSED, add the explicit early gate and remove Session credentials/signaling exposure. Do not invent a relay or assume cooperative-browser trust. Update schemas/API accordingly. Delete obsolete Session-bound direct-token behavior tests and replace them with authority/fail-closed assertions.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_voice_live_webrtc.py -vv --cov=app.services.voice_live_webrtc --cov=app.api.voice_live --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/voice_live_webrtc.py'; .\.venv\Scripts\ruff.exe check app/services/voice_live_webrtc.py app/api/voice_live.py app/schemas/voice_live.py tests/test_voice_live_webrtc.py; .\.venv\Scripts\ruff.exe format --check app/services/voice_live_webrtc.py app/api/voice_live.py app/schemas/voice_live.py tests/test_voice_live_webrtc.py</automated></verify>
  <done>Backend exactly implements the approved safe verdict with 100% changed service coverage and no Session credential/context exposure.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Remove browser response authority and encode supported/unsupported UX contract</name>
  <files>frontend/src/api/voice-live.ts, frontend/src/types/voice-live.ts, frontend/src/hooks/use-voice-live-webrtc.ts, frontend/src/api/voice-live.test.ts, frontend/src/hooks/use-voice-live-webrtc.test.ts</files>
  <behavior>
    - Session-bound calls send only session ID plus required SDP/media payload under PROVEN topology.
    - Session-bound browser never sends `session.update` or `response.create`, receives no token/control URL/context/pin/tools.
    - FAIL-CLOSED topology surfaces the structured unsupported error and does not silently select another transport.
    - Standalone playground behavior is not broadened into training authority.
  </behavior>
  <action>Refactor API/types/hook to match the backend verdict. Pass Unified Training `sessionId` correctly (the current hook omits it in `fetchWebRTCSession`). Remove Session-bound direct Azure signaling and `sendTextMessage()` bare response creation. Preserve direct RTP only if PROVEN. Keep user-facing errors localized through existing handling; no new feature scope.</action>
  <verify><automated>Set-Location frontend; npm run test -- src/api/voice-live.test.ts src/hooks/use-voice-live-webrtc.test.ts --coverage --coverage.include=src/api/voice-live.ts --coverage.include=src/hooks/use-voice-live-webrtc.ts; npx tsc -b</automated></verify>
  <done>Frontend has no Session response/context authority and follows the proven or fail-closed backend contract with focused coverage.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Browser → WebRTC API | Session ID/SDP/media only, ownership check |
| Browser data channel → Azure | Enforce proven prevention or disable Session WebRTC |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-05-01 | Elevation of Privilege | Data channel | mitigate | Proven service policy or fail-closed gate |
| T-31-05-02 | Information Disclosure | Broker response | mitigate | No Azure credentials/control URL/context for Session |
| T-31-05-03 | Tampering | Client response events | mitigate | Remove Session response.create/session.update paths |
| T-31-05-04 | Spoofing | Session | mitigate | JWT ownership and exact pin resolution before action |
</threat_model>
<verification>Backend/frontend focused tests, 100% changed service coverage, Ruff, and TypeScript pass.</verification>
<success_criteria>WebRTC is equivalent and server-authoritative only when proven; otherwise it is explicitly unavailable with no fallback.</success_criteria>
<output>Create `31-05-SUMMARY.md`; do not commit/push.</output>
