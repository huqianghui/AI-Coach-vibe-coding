---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified:
  - backend/app/services/session_turn_orchestrator.py
  - backend/app/services/agent_chat_service.py
  - backend/app/services/session_service.py
  - backend/app/api/sessions.py
  - backend/tests/test_session_turn_orchestrator.py
  - backend/tests/test_agent_chat_service.py
  - backend/tests/test_session_service.py
  - backend/tests/test_sessions_api.py
autonomous: true
requirements: [R2-TEXT, R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "Every text initial/continuation response receives current server instructions and exact Session pin"
    - "Successful response persistence precedes monotonic progression for the next response"
    - "Context/detector failures never invoke fallback or mutate the continuation/progression state"
  artifacts:
    - path: backend/app/services/session_turn_orchestrator.py
      provides: Exact-Agent text turn ordering and progression commit
    - path: backend/app/services/agent_chat_service.py
      provides: Responses top-level instructions on sync/stream paths
  key_links:
    - from: backend/app/api/sessions.py
      to: backend/app/services/session_turn_orchestrator.py
      via: owned active Session text turn
      pattern: "session_turn_orchestrator"
    - from: backend/app/services/agent_chat_service.py
      to: Foundry Responses
      via: instructions + exact agent_reference + previous_response_id
      pattern: "instructions.*agent_reference.*previous_response_id"
---
<objective>Integrate the canonical context and next-turn SOP progression into exact pinned-Agent text streaming.</objective>
<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-AUDIT-DECISION.md
@backend/app/services/session_skill_context.py
@backend/app/services/agent_chat_service.py
@backend/app/api/sessions.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add temporary instructions to both Responses paths</name>
  <files>backend/app/services/agent_chat_service.py, backend/tests/test_agent_chat_service.py</files>
  <behavior>
    - `chat_with_agent()` and `stream_agent_response()` require non-empty server instructions.
    - Initial and continued calls send `instructions`; continuation also sends unchanged `previous_response_id`.
    - Exact `agent_reference.name/version/type` remains unchanged; no tools/tool_choice or fallback appears.
    - Invalid instructions fail before client/network lookup.
  </behavior>
  <action>Extend `_build_openai_request()`, `chat_with_agent()`, and `stream_agent_response()` with a required keyword-only `instructions` value proven by Plan 31-01. Update call sites/tests. Never make it optional for Session-bound calls; preserve exact model field required by API but never use it as an identity fallback (D-01, D-04, D-07).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_agent_chat_service.py -vv --cov=app.services.agent_chat_service --cov-report=term-missing --cov-fail-under=100; .\.venv\Scripts\ruff.exe check app/services/agent_chat_service.py tests/test_agent_chat_service.py; .\.venv\Scripts\ruff.exe format --check app/services/agent_chat_service.py tests/test_agent_chat_service.py</automated></verify>
  <done>Both Responses implementations always carry tested temporary instructions while retaining exact pin, continuation, and Agent-owned tools.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Orchestrate current-context response then next-step commit</name>
  <files>backend/app/services/session_turn_orchestrator.py, backend/app/services/session_service.py, backend/tests/test_session_turn_orchestrator.py, backend/tests/test_session_service.py</files>
  <behavior>
    - Freeze envelope N before Agent call; use step N for response.
    - On terminal success persist assistant/response ID/audit metadata, then evaluate authoritative user input and commit only `max(N, detected)` for the next turn.
    - Duplicate/replayed turn key is idempotent; detector indeterminate/failed keeps N.
    - Agent/context failure leaves assistant response ID and SOP step unchanged.
  </behavior>
  <action>Create a service orchestration contract consumed by the SSE route. Centralize order and compare-and-set/idempotency using the audit mechanism chosen in 31-AUDIT-DECISION.md. Remove `update_sop_progress()` heuristic mutation or turn it into the tested monotonic commit helper; never rewrite `focus_instruction` or refetch latest Skill content (D-05, D-06). If audit decision required a not-yet-planned table, stop and revise before edits.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_turn_orchestrator.py tests/test_session_service.py -vv --cov=app.services.session_turn_orchestrator --cov=app.services.session_service --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/session_turn_orchestrator.py'; .\.venv\Scripts\ruff.exe check app/services/session_turn_orchestrator.py app/services/session_service.py tests/test_session_turn_orchestrator.py tests/test_session_service.py</automated></verify>
  <done>Turn ordering, failure, replay, monotonic progression, and next-turn semantics are fully tested.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Wire SSE route without changing browser contract</name>
  <files>backend/app/api/sessions.py, backend/tests/test_sessions_api.py</files>
  <behavior>
    - Browser request remains `{message}` only.
    - Owned active Session is resolved before context; errors are structured and no SSE Agent output starts on context failure.
    - Existing text/key_messages/hint/done UX remains after successful orchestration.
  </behavior>
  <action>Delegate the Agent portion of `send_message()` to the orchestrator while keeping router HTTP/SSE concerns and existing suggestion/key-message behavior. Do not accept context/pin/tools/continuation fields and do not add conference/scoring changes.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_sessions_api.py -vv --cov=app.api.sessions --cov-report=term-missing; .\.venv\Scripts\ruff.exe check app/api/sessions.py tests/test_sessions_api.py; .\.venv\Scripts\ruff.exe format --check app/api/sessions.py tests/test_sessions_api.py</automated></verify>
  <done>Text user story is context-guided with unchanged browser input and existing SSE observables.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Browser → message API | Schema accepts message only; Session owns all policy |
| Orchestrator → Responses | Required rendering + exact pin + no tool override |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-03-01 | Tampering | Request context | mitigate | No browser context fields; build from owned Session |
| T-31-03-02 | Repudiation | Turn ordering | mitigate | Durable turn key/digest/response metadata |
| T-31-03-03 | Tampering | Continuation | mitigate | Server-owned response ID only, update after completion |
| T-31-03-04 | Elevation of Privilege | Tools | mitigate | Assert no request tools/tool_choice override |
</threat_model>
<verification>All focused tests and Ruff pass; each newly introduced module reaches 100% coverage before Plan 31-04.</verification>
<success_criteria>Text behavior uses current temporary context on every exact-Agent response and progresses only for the next turn.</success_criteria>
<output>Create `31-03-SUMMARY.md`; do not commit/push.</output>
