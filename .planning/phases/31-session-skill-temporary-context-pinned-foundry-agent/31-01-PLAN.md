---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_phase31_live_capabilities.py
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
autonomous: false
requirements: [R2-TEXT, R2-WS, R2-WEBRTC, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry and Voice Live
    why: Non-mutating live capability proof against ai-coach-demo
    env_vars:
      - name: AZURE_FOUNDRY_ENDPOINT
        source: Existing Phase 30 live environment
      - name: AZURE_FOUNDRY_DEFAULT_PROJECT
        source: Must equal ai-coach-demo
      - name: UNIFIED_TRAINING_KB_QUESTION
        source: Existing Phase 30 operator-provided IQ-only question
      - name: UNIFIED_TRAINING_KB_EXPECTED_MARKER
        source: Existing Phase 30 operator-provided marker
must_haves:
  truths:
    - "Live text proves top-level instructions, exact Dr-Chen-Jun v5, continuation context replacement, and IQ in one chain"
    - "Live Voice Live proves response-level additional_instructions with VAD auto-response disabled on exact Agent v5"
    - "Live WebRTC evidence determines whether backend authority and browser bypass prevention are feasible"
    - "No probe invokes an Agent resource write operation"
  artifacts:
    - path: backend/tests/integration/test_phase31_live_capabilities.py
      provides: Opt-in non-mutating text, WS, and WebRTC live probes
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
      provides: Sanitized capability verdicts and later-plan revision gate
  key_links:
    - from: backend/tests/integration/test_phase31_live_capabilities.py
      to: Dr-Chen-Jun version 5 in ai-coach-demo
      via: exact immutable references and session/response operations only
      pattern: "Dr-Chen-Jun.*5"
---

<objective>
Run the mandatory read-only capability gate before production integration.

Purpose: Replace SDK-surface assumptions with behavioral evidence for exact-Agent temporary context, native IQ retention, Voice Live response authority, and WebRTC bypass safety.
Output: Opt-in integration probes plus a blocking evidence checkpoint that may revise Plans 31-02 through 31-07.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-RESEARCH.md
@.planning/phases/30-unified-training-pinned-foundry-hcp-agent-kb-retrieval/30-ACCEPTANCE.md
@backend/tests/integration/test_unified_training_foundry_kb.py
@backend/app/services/agent_chat_service.py
@backend/app/services/voice_live_websocket.py
@backend/app/services/voice_live_webrtc.py

<interfaces>
`chat_with_agent()` currently builds exact `agent_reference` and supports `previous_response_id`, but not `instructions`. Voice Live SDK is pinned to `azure-ai-voicelive==1.3.0b1`, API `2026-07-15`. Production files must not be edited in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add guarded read-only live probe harness</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py</files>
  <behavior>
    - Missing opt-in/env inputs skip during ordinary suites, while the explicit release command treats any skip as failure.
    - Constants assert project `ai-coach-demo`, Agent `Dr-Chen-Jun`, version `5` before network calls.
    - A write-operation guard fails if test code exposes/calls Agent create/update/delete/publish/version-create methods.
    - Evidence redacts credentials, URLs with tokens, raw temporary instructions, and raw Skill content.
  </behavior>
  <action>Create integration-only helpers for random benign behavior markers, sanitized evidence, exact pin assertions, IQ tool inspection, event collection, timeouts, and an explicit Agent-resource-write guard. Reuse Phase 30 credential/session patterns. Permit only read Agent inspection and ephemeral Responses/Voice Live/WebRTC session operations. Do not import or call production Agent sync/write helpers; do not alter production code, local DB state, or Azure Agent resources (D-04, D-10, D-13).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py --collect-only -q; .\.venv\Scripts\ruff.exe check tests/integration/test_phase31_live_capabilities.py; .\.venv\Scripts\ruff.exe format --check tests/integration/test_phase31_live_capabilities.py</automated></verify>
  <done>Probe module collects, is lint-clean, contains hard exact-target/no-write guards, and is inert unless explicitly opted in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove text, Voice Live, and WebRTC capabilities</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md</files>
  <behavior>
    - Text Probe A sends top-level `instructions` with exact `agent_reference`; turn 2 uses `previous_response_id` plus a changed behavior marker.
    - Text Probe B proves the behavior constraint and IQ-only marker/tool evidence in the same chain without app-supplied tools.
    - Voice Probe C configures `AzureSemanticVad(create_response=False)`, proves committed input does not auto-create, then uses response-level `additional_instructions`; run voice and avatar-capable variants and require exact Agent/IQ evidence.
    - WebRTC Probe D retains backend sideband/control, creates a context-guided response, attempts browser/data-channel `response.create`, and records whether the service enforceably rejects/prevents bypass while preserving lifecycle/transcript events.
  </behavior>
  <action>Implement the four probes against only `ai-coach-demo` / `Dr-Chen-Jun` / `5`. Behavioral assertions must not ask the Agent to reveal hidden instructions verbatim. Record timestamp, SDK/API version, exact pin, mode, response IDs, digest/marker compliance, `knowledge_base_retrieve` evidence, bypass result, and `agent_resource_writes: 0`; never record secrets or raw policy. Execute with `PHASE31_LIVE_PROBES=1`. Write PASS/FAIL/UNSUPPORTED verdicts. A 2xx without behavior is failure. Do not weaken a failed result with model/latest/generic fallback.</action>
  <verify><automated>Set-Location backend; $env:PHASE31_LIVE_PROBES='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py -m integration -vv -rs | Tee-Object -FilePath phase31-live-capabilities.log; if (Select-String phase31-live-capabilities.log -Pattern 'skipped|SKIPPED' -Quiet) { exit 1 }; if (Select-String phase31-live-capabilities.log -Pattern 'failed|FAILED' -Quiet) { exit 1 }</automated></verify>
  <done>Evidence contains explicit text, WS/voice, WS/avatar, and WebRTC authority/bypass verdicts, exact v5 + IQ proof, and zero resource writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Non-mutating live capability evidence for the exact pinned Agent across all Requirement 2 transports.</what-built>
  <how-to-verify>Review 31-CAPABILITY-EVIDENCE.md. Confirm exact project/name/version, behavior+IQ proof, zero Agent resource writes, Voice VAD no-auto-response, and WebRTC bypass verdict. If any asserted temporary-instruction capability failed or WebRTC bypass is possible, revise the affected later plans before executing 31-02; WebRTC must select explicit fail-closed behavior rather than fallback.</how-to-verify>
  <resume-signal>Type "capability gate approved" and identify WebRTC verdict as PROVEN or FAIL-CLOSED; otherwise describe required plan revisions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Test process → live Azure | Exact target assertions, opt-in flag, read/session operations only |
| Live output → evidence | Redaction and hashes; no credentials/raw instructions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-01-01 | Tampering | Agent resource | mitigate | Write-operation guard; zero writes asserted and recorded |
| T-31-01-02 | Spoofing | Target Agent | mitigate | Hard assert ai-coach-demo / Dr-Chen-Jun / 5 before calls |
| T-31-01-03 | Information Disclosure | Evidence | mitigate | Sanitized IDs/digests only; redact tokenized URLs and raw context |
| T-31-01-04 | Elevation of Privilege | WebRTC browser channel | mitigate | Active bypass attempt determines PROVEN versus FAIL-CLOSED |
</threat_model>

<verification>Collection, Ruff, all opt-in live probes with zero skip/fail, and manual evidence checkpoint must pass in order.</verification>
<success_criteria>The live service—not mocks—supports each enabled integration; unsafe/unsupported WebRTC is explicitly identified before production work.</success_criteria>
<output>After completion, create `31-01-SUMMARY.md`. Do not commit or push; Plan 31-07 owns the single Requirement 2 release.</output>
