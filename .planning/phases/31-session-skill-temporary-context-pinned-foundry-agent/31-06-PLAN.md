---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 06
type: execute
wave: 6
depends_on: [31-05]
files_modified:
  - frontend/src/hooks/use-voice-live.ts
  - frontend/src/hooks/use-voice-live.test.ts
  - frontend/src/pages/user/unified-session.test.tsx
  - frontend/e2e/unified-training-session-skill-context.spec.ts
autonomous: true
requirements: [R2-TEXT, R2-WS, R2-WEBRTC, R2-SOP, R2-TRUST]
must_haves:
  truths:
    - "Unified Training browser sends user input/media and session_id only, never server context authority"
    - "Text user story observes step-N then step-N+1 guided behavior without browser context fields"
    - "WS/avatar never send Session-bound response.create and WebRTC follows its proven/fail-closed verdict"
    - "Playwright covers the complete Requirement 2 user story"
  artifacts:
    - path: frontend/e2e/unified-training-session-skill-context.spec.ts
      provides: Requirement 2 cross-transport browser trust and behavior story
    - path: frontend/src/hooks/use-voice-live.ts
      provides: Session-bound WS client intent contract without response authority
  key_links:
    - from: frontend/src/hooks/use-voice-live.ts
      to: /api/v1/voice-live/ws
      via: first frame with session_id only and no response.create
      pattern: "session_id"
---
<objective>Remove remaining browser response authority and prove Requirement 2 through frontend unit and Playwright user stories.</objective>
<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@frontend/src/hooks/use-voice-live.ts
@frontend/src/pages/user/unified-session.tsx
@frontend/e2e/unified-training-pinned-agent.spec.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make the WS browser contract intent-only</name>
  <files>frontend/src/hooks/use-voice-live.ts, frontend/src/hooks/use-voice-live.test.ts, frontend/src/pages/user/unified-session.test.tsx</files>
  <behavior>
    - Session first frame remains exactly `{type:'session.update', session:{session_id}}`.
    - Session text intent sends `conversation.item.create` only; backend controls response creation.
    - Raw Session send cannot transmit response/session instruction/tool/identity override events.
    - Existing standalone playground call shape remains separately tested.
  </behavior>
  <action>Add a Session-bound client intent guard and remove the bare `response.create` after Session text input. Ensure audio commit behavior relies on backend-authoritative turn orchestration. Do not expose context in types/options. Update Unified Session tests for WS, avatar, and WebRTC verdict behavior.</action>
  <verify><automated>Set-Location frontend; npm run test -- src/hooks/use-voice-live.test.ts src/pages/user/unified-session.test.tsx --coverage --coverage.include=src/hooks/use-voice-live.ts; npx tsc -b</automated></verify>
  <done>Session-bound frontend cannot author response context or trigger an unskilled response.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add the Requirement 2 Playwright story</name>
  <files>frontend/e2e/unified-training-session-skill-context.spec.ts</files>
  <behavior>
    - Text two-turn SSE displays behavior for committed step 0 then advanced next step while request bodies contain only user message.
    - Voice/Avatar first frame contains only session_id and subsequent frames contain no forbidden keys/events.
    - WebRTC scenario asserts the approved server-owned request contract or explicit unsupported response with no fallback.
    - Network/WebSocket audit rejects Agent identity, Skill/focus text, SOP state, instructions/additional_instructions, tools/tool_choice, continuation, and Session-bound response.create.
  </behavior>
  <action>Create a separate Requirement 2 spec; do not modify Phase 30 acceptance spec. Mock server behavior only for browser contract/UX, clearly distinguishing it from live Agent proof. Cover Chinese-visible success/error state as currently rendered and both voice and avatar Session modes.</action>
  <verify><automated>Set-Location frontend; npx playwright test --config=e2e/playwright.config.ts unified-training-session-skill-context.spec.ts --project=chromium --reporter=line</automated></verify>
  <done>Playwright reports nonzero passed, zero failed, zero skipped and proves browser non-authority across all transports.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Browser app → API/WS | Network/frame audit and intent-only client guards |
| Mock E2E → acceptance claim | Explicitly limited to browser contract; live proof remains Plan 31-01/31-07 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-06-01 | Tampering | Browser frames | mitigate | Forbidden-key/event assertions and send guard |
| T-31-06-02 | Spoofing | Agent identity | mitigate | No identity fields in any client request |
| T-31-06-03 | Repudiation | E2E scope | mitigate | Separate mocked browser evidence from live capability evidence |
</threat_model>
<verification>Focused Vitest coverage, TypeScript, and Requirement 2 Playwright all pass before final gates.</verification>
<success_criteria>The browser is demonstrably non-authoritative and the primary two-turn/cross-transport user story is covered.</success_criteria>
<output>Create `31-06-SUMMARY.md`; do not commit/push.</output>
