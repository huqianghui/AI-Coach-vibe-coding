---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 02
type: execute
status: ready-alternative-text-capability-gate
wave: 2
depends_on: [31-01]
files_modified:
  - backend/tests/integration/test_phase31_live_capabilities.py
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-02-PLAN.revised.md
autonomous: false
requirements: [R2-TEXT, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry Responses and Conversations
    why: Non-mutating live capability proof against the configured exact Session pin
    env_vars:
      - name: UNIFIED_TRAINING_KB_QUESTION
        source: Existing Phase 30 operator-provided IQ-only question; value must not be written to evidence
      - name: UNIFIED_TRAINING_KB_EXPECTED_MARKER
        source: Existing Phase 30 operator-provided expected marker; value must not be written to evidence
    dashboard_config: []
must_haves:
  truths:
    - "The alternative text gate tries candidate context surfaces strictly in order: per-response typed developer input, per-response typed system input, disposable Conversation developer item, disposable Conversation system item, then server-prefixed user input"
    - "A candidate is viable only when initial A and continuation B exhibit distinct required behavior and each response has a correlated successful knowledge_base_retrieve call"
    - "Every request uses only Dr-Chen-Jun version 5 through exact agent_reference, supplies no tools/tool_choice, and never retries rejected top-level instructions"
    - "No database, production file, Agent definition/version/tool configuration, protected path, Phase 30 artifact, Voice surface, avatar surface, or WebRTC surface is modified"
    - "Exact Agent definition/tool and sorted version-inventory fingerprints match before/after, Agent write count is zero, and disposable Conversations are cleaned up"
    - "Voice WS/avatar remain deferred and fail-closed because the established endpoint returned 404; Session WebRTC remains fail-closed"
  artifacts:
    - path: backend/tests/integration/test_phase31_live_capabilities.py
      provides: Sequential alternative text-only live capability gate with deterministic verdict and cleanup
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
      provides: Sanitized candidate-by-candidate results, chosen deterministic verdict, correlations, and immutability proof
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md
      provides: Updated capability-gate summary that keeps production implementation blocked
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-02-PLAN.revised.md
      provides: Sole executable continuation plan for the alternative text capability gate
  key_links:
    - from: backend/tests/integration/test_phase31_live_capabilities.py
      to: OpenAI Responses typed input contract
      via: EasyInputMessageParam role developer/system followed by a typed user message
      pattern: "EasyInputMessageParam|developer|system"
    - from: backend/tests/integration/test_phase31_live_capabilities.py
      to: OpenAI Conversations item contract
      via: disposable conversation plus typed Message item and guaranteed delete cleanup
      pattern: "conversations\\.(create|items|delete)"
    - from: backend/tests/integration/test_phase31_live_capabilities.py
      to: Dr-Chen-Jun version 5 native Foundry IQ
      via: exact agent_reference and correlated response.mcp_call lifecycle
      pattern: "knowledge_base_retrieve|response\\.mcp_call"
---

<objective>
Run one alternative, text-only Requirement 2 capability gate after live evidence disproved top-level `instructions` with exact `agent_reference`.

Purpose: Determine whether any allowed request/session input surface can temporarily guide the exact pinned Foundry Agent while preserving continuation behavior and its native Foundry IQ invocation, without authorizing production implementation.
Output: One deterministic text verdict, candidate matrix, correlation evidence, matching immutable-resource fingerprints, protected-work proof, and a blocking human checkpoint.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.revised.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-RESEARCH-ADDENDUM.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md
@backend/tests/integration/test_phase31_live_capabilities.py
@backend/app/services/agent_chat_service.py

<interfaces>
Production files are read-only. The current `agent_chat_service.py` sends `input=[{"role": "user", "content": message}]`, exact `extra_body.agent_reference`, and optional `previous_response_id`; it does not expose a Session temporary-context contract.

Installed OpenAI SDK 2.38.0 contracts verified during planning:

```python
from openai.types.responses.easy_input_message_param import EasyInputMessageParam

# TypedDict requires content and role; role accepts user, assistant, system, developer.
# `type` may be the literal "message".
developer_item: EasyInputMessageParam = {
    "type": "message",
    "role": "developer",
    "content": directive,
}
```

```python
from openai.types.responses.response_input_item_param import Message

# Conversation item creation accepts Iterable[ResponseInputItemParam].
conversation_item: Message = {
    "type": "message",
    "role": "developer",
    "content": [{"type": "input_text", "text": directive}],
}
```

The installed client exposes `conversations.create(items=...)`, `conversations.items.create(conversation_id, items=...)`, and `conversations.delete(conversation_id)`. Conversation creation/item insertion/deletion is allowed only as disposable probe state with guaranteed cleanup; it is not an Agent write and must never be presented as production authorization.
</interfaces>

<decision_coverage>
| Decision | Task | Coverage |
|---|---|---|
| D-01 exact immutable pin | Task 1, Task 2 | Full |
| D-02 server-owned context | Task 1, Task 2 | Full for capability evidence only |
| D-03 behavioral injection | Task 1, Task 2 | Full for capability verdict |
| D-04 preserve IQ/no Agent mutation | Task 1, Task 2 | Full |
| D-08 text surface research superseded by live rejection | Task 1, Task 2 | Full alternative gate; top-level instructions prohibited |
| D-13 correlated actual IQ invocation | Task 1, Task 2 | Full |
| D-14 blocking sanitized preflight | Task 1, Task 2 | Full |
| D-15 capability-first regeneration | Checkpoint | Full; production remains blocked |
| D-17 protected work | Task 1, Task 2 | Full |

D-05–D-07, D-09–D-12, D-16, and D-18 are production architecture/release decisions and are deliberately not implemented by this capability-only plan. Voice WS/avatar stay deferred/fail-closed after 404, and WebRTC stays fail-closed. No deferred feature is introduced.
</decision_coverage>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regenerate the probe as a strict sequential alternative-text gate</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py</files>
  <behavior>
    - Preflight resolves an already-existing Skill-bound `CoachingSession` pinned exactly to project `ai-coach-demo`, Agent `Dr-Chen-Jun`, version `5`; if none exists, the gate blocks without calling `create_session()`, flushing, committing, or changing a database.
    - The probe never sends top-level `instructions` and never runs Voice Live, avatar, or WebRTC calls.
    - Candidate 1 sends typed `EasyInputMessageParam` items `[developer A, user question]`, then `[developer B, user question]` with `previous_response_id` from A.
    - Only if Candidate 1 is non-viable, Candidate 2 repeats the same per-response continuation contract with typed `system` A/B items.
    - Only if both per-response candidates are non-viable, Candidate 3 creates one disposable Conversation, inserts/uses a typed developer A item for the initial response, then appends a typed developer B item for the continued response on the same Conversation; Candidate 4 repeats with system items only if Candidate 3 is non-viable.
    - Only if all higher-priority candidates are non-viable, Candidate 5 sends a server-constructed, clearly delimited user message containing directive A plus the IQ question, then directive B plus the IQ question with `previous_response_id` from A.
    - Every candidate sends exact `extra_body.agent_reference={name: Dr-Chen-Jun, version: "5", type: agent_reference}`, the configured model only because the API requires it, and no `tools` or `tool_choice` at any nesting level.
    - A viable candidate requires response A to obey only marker A, response B to obey only marker B and not marker A, a real continuation link (same chain through `previous_response_id` or the same explicit Conversation), and a successful exact `knowledge_base_retrieve` MCP lifecycle correlated separately to both response IDs.
    - Accepted IQ proof requires exact call name, call ID, response ID/chain correlation, successful terminal `response.mcp_call.completed` or SDK-exposed successful final `mcp_call`, and no matching failed event; answer text and the configured Agent tool list are insufficient.
    - Candidate failures are sanitized and classified as `REJECTED`, `BEHAVIOR_FAILED`, `IQ_FAILED`, `CORRELATION_FAILED`, or `CLEANUP_FAILED`; the first fully viable candidate stops further candidate execution.
  </behavior>
  <action>Refactor only the existing Phase 31 probe. Remove the `create_session` import and fallback so the test is DB-read-only. Split text execution into typed candidate builders and one shared stream collector. Preserve the existing exact Agent read facade, static/runtime Agent-write traps, full canonical Agent definition/tool hash, sorted version-inventory hash, protected-file hash manifest, and secret redaction. Strengthen MCP parsing so every call is keyed by `(response_id, call_id)`, terminal success is tied to that same response, and both A and B responses must independently prove `knowledge_base_retrieve`. Record sanitized response/call IDs but never directive text, IQ question, expected marker, credentials, or tokenized URLs.

For explicit Conversation candidates, use a newly created probe-only Conversation, record its sanitized ID, add typed instruction items through the SDK, use that same Conversation for initial/continued responses, and execute deletion in `finally` under the named cleanup deadline. Treat create/add/delete failures as blocking evidence; do not leave disposable Conversations intentionally. Conversation operations are the only allowed disposable server-resource writes; all Agent write methods remain trapped and counted.

Define one final text verdict enum/string exactly as one of: `PROVEN: RESPONSE_INPUT_DEVELOPER`, `PROVEN: RESPONSE_INPUT_SYSTEM`, `PROVEN: CONVERSATION_ITEM_DEVELOPER`, `PROVEN: CONVERSATION_ITEM_SYSTEM`, `PROVEN: SERVER_PREFIXED_USER`, or `BLOCKED: NO VIABLE TEXT TEMPORARY CONTEXT SURFACE`. Do not use PASS based only on HTTP success. Keep established `Voice WS verdict: BLOCKED: ENDPOINT 404`, `Avatar verdict: BLOCKED: ENDPOINT 404`, and `WebRTC verdict: FAIL-CLOSED` as non-executed inherited verdicts (D-01–D-04, D-08, D-13, D-14, D-17).</action>
  <verify>
    <automated>Set-Location backend; $PSNativeCommandUseErrorActionPreference=$true; &amp; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py --collect-only -q; $code=$LASTEXITCODE; if ($code -ne 0) { exit $code }; &amp; .\.venv\Scripts\ruff.exe check tests/integration/test_phase31_live_capabilities.py; $code=$LASTEXITCODE; if ($code -ne 0) { exit $code }; &amp; .\.venv\Scripts\ruff.exe format --check tests/integration/test_phase31_live_capabilities.py; exit $LASTEXITCODE</automated>
  </verify>
  <done>The probe collects and is lint/format clean; it is text-only, database-read-only, Agent-read-only, ordered exactly by the five candidate families, requires per-response A/B plus IQ correlation, and has guaranteed Conversation cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the live text matrix and write deterministic sanitized evidence</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md</files>
  <behavior>
    - The explicit live command produces zero pytest skips and exits zero only for one of the five `PROVEN` text verdicts; no viable candidate exits nonzero with the exact `BLOCKED` verdict while still writing evidence.
    - Evidence lists candidate order, attempted/not-attempted status, sanitized rejection/failure class, A/B response IDs, continuation mechanism, correlated successful call IDs for both A and B, observed accepted event types, no tools override, exact pin, and selected verdict.
    - Evidence carries the previously observed top-level-instructions 400 rejection as historical input and proves the regenerated probe did not retry that surface.
    - Before/after exact Agent definition/tool and version inventory fingerprints match, static guard is clean, runtime Agent write count is zero, disposable Conversation cleanup is `NOT NEEDED` or `CONFIRMED`, protected hashes match, and Git status contains no database/sidecar/protected/Phase 30 changes caused by the gate.
    - Summary explicitly states that the verdict authorizes only subsequent GSD replanning; it does not authorize production code/tests, schema/migrations, database changes, commit, or push. Withdrawn 31-02 through 31-07 remain non-executable.
  </behavior>
  <action>Run the focused live probe once with the explicit opt-in. The test must always render sanitized evidence in `finally`, then fail if its deterministic verdict is `BLOCKED`, fingerprints differ, Agent write count is nonzero, cleanup is not confirmed when used, protected hashes differ, or the exact pin differs. Do not create a log file or any fifth artifact. Update the existing evidence and existing 31-01 summary only; do not create another summary.

The evidence must retain Voice WS/avatar as deferred/fail-closed due the established 404 and retain WebRTC as fail-closed. It must not imply that a text success proves Voice/avatar/WebRTC. If a higher-priority candidate is proven, mark lower candidates `NOT ATTEMPTED: FIRST VIABLE SURFACE FOUND`; if all fail, report every classified failure and the single blocked verdict. Do not implement the winning surface in production and do not edit the withdrawn plan files.</action>
  <verify>
    <automated>Set-Location backend; $env:PHASE31_TEXT_ALT_GATE='1'; $PSNativeCommandUseErrorActionPreference=$true; $output = &amp; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py -m integration -vv -rs 2&gt;&amp;1; $code=$LASTEXITCODE; $output | ForEach-Object { Write-Output $_ }; if ($code -ne 0) { exit $code }; if (($output | Out-String) -match '(?im)^.*SKIPPED.*$') { exit 1 }; Set-Location ..; $e='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md'; $s='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md'; $c=Get-Content $e -Raw; $u=Get-Content $s -Raw; if ($c -notmatch 'Text Responses verdict: PROVEN: (RESPONSE_INPUT_DEVELOPER|RESPONSE_INPUT_SYSTEM|CONVERSATION_ITEM_DEVELOPER|CONVERSATION_ITEM_SYSTEM|SERVER_PREFIXED_USER)') { exit 1 }; if ($c -notmatch 'Exact Agent pin: Dr-Chen-Jun/5' -or $c -notmatch 'Request tools/tool_choice supplied: false' -or $c -notmatch 'Agent resource writes: 0' -or $c -notmatch 'Definition/tool fingerprint: MATCH' -or $c -notmatch 'Version inventory fingerprint: MATCH' -or $c -notmatch 'Protected hash manifest: MATCH' -or $c -notmatch 'Disposable Conversation cleanup: (NOT NEEDED|CONFIRMED)' -or $c -notmatch 'A correlated successful knowledge_base_retrieve: True' -or $c -notmatch 'B correlated successful knowledge_base_retrieve: True') { exit 1 }; if ($u -notmatch 'production.*not authorized' -or $u -notmatch '31-02 through 31-07.*non-executable' -or $u -notmatch 'commit.*push.*not authorized') { exit 1 }</automated>
  </verify>
  <done>One live-proven text alternative is recorded with distinct A/B continuation behavior, two correlated exact IQ calls, exact pin/no-tools/no-write proof, cleanup and matching fingerprints; otherwise execution stops with deterministic blocked evidence and no production authorization.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A regenerated, alternative text-only capability verdict for Requirement 2. No production implementation or release work was performed.</what-built>
  <how-to-verify>
1. Open `31-CAPABILITY-EVIDENCE.md` and confirm the candidate sequence was developer input → system input → disposable Conversation developer/system → server-prefixed user, with lower candidates not attempted only after the first viable result.
2. If the verdict is `PROVEN`, confirm both A and B have distinct behavior, a real continuation link, separately correlated successful `knowledge_base_retrieve` calls, exact `Dr-Chen-Jun/5`, no tools override, zero Agent writes, matching Agent/version/protected fingerprints, and confirmed/not-needed Conversation cleanup.
3. If the verdict is `BLOCKED`, confirm all five candidate families have deterministic sanitized failure classes and no success is inferred from 2xx or answer text.
4. Confirm Voice WS/avatar remain blocked after 404, WebRTC remains fail-closed, production implementation/commit/push are not authorized, and original withdrawn 31-02 through 31-07 were not overwritten.
  </how-to-verify>
  <resume-signal>If and only if a PROVEN verdict satisfies every check, type "alternative text capability evidence approved; regenerate production plans from [exact verdict]". If BLOCKED or any invariant is missing, type "alternative text capability gate blocked; no production planning or implementation authorized" and list the failed invariant.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing DB/config → probe | Read-only resolution of exact Session/config; no fallback Session creation or DB persistence |
| Probe → Foundry Responses | Exact Agent pin, typed input candidates, no tools override, bounded timeouts |
| Probe → disposable Conversations | Probe-only create/item/delete lifecycle with guaranteed cleanup and no Agent writes |
| Foundry stream → evidence | Response/call correlation, exact terminal MCP event, redaction, deterministic classification |
| Workspace → evidence update | Four-file allowlist, protected hashes, no Phase 30/DB/protected changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-02R-01 | Spoofing | Session/Agent target | mitigate | Require existing Skill-bound Session and assert `ai-coach-demo / Dr-Chen-Jun / 5` before requests |
| T-31-02R-02 | Tampering | Agent definition/tools/versions | mitigate | Read-only Agent facade, static/runtime write traps, exact canonical before/after fingerprints |
| T-31-02R-03 | Repudiation | Claimed temporary-context behavior | mitigate | Distinct A/B markers, explicit continuation mechanism, sanitized response IDs, deterministic candidate verdicts |
| T-31-02R-04 | Repudiation | Native IQ invocation | mitigate | Require successful exact `knowledge_base_retrieve` call correlated independently to both response IDs |
| T-31-02R-05 | Information Disclosure | Evidence/output | mitigate | Never persist directives, IQ question/marker, secrets, credentials, tokenized URLs, or raw Agent policy |
| T-31-02R-06 | Denial of Service | Streams/cleanup | mitigate | Named connection/behavior/MCP/cleanup deadlines and stop after first viable surface |
| T-31-02R-07 | Tampering | Disposable Conversation | mitigate | Unique probe Conversation, `finally` deletion, cleanup failure blocks verdict |
| T-31-02R-08 | Elevation of Privilege | User-prefix fallback | mitigate | Last-resort ordering, server-only construction, exact behavioral/IQ proof, no browser authority claim |
</threat_model>

<verification>
Collection, Ruff check, and Ruff format must pass before the focused live run. The live run must have zero skips and may pass only with one exact `PROVEN` verdict plus all behavior/IQ/pin/no-tools/no-write/fingerprint/cleanup invariants. A deterministic `BLOCKED` result is a valid evidence outcome but intentionally fails execution and forbids production planning/implementation.
</verification>

<success_criteria>
The exact pinned Agent has one evidence-backed alternative text temporary-context surface, or all permitted surfaces are deterministically blocked. In either outcome, no production code/test, database, protected path, Phase 30 file, Agent resource, Voice/avatar/WebRTC path, commit, or push is changed or authorized.
</success_criteria>

<output>
Update only `31-CAPABILITY-EVIDENCE.md` and `31-01-SUMMARY.md` after modifying the existing probe. Do not create another summary. Do not modify ROADMAP/STATE, production code/tests, databases, protected paths, Phase 30 files, or original withdrawn 31-02 through 31-07. Do not commit or push.
</output>
