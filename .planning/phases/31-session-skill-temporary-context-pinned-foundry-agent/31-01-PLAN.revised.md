---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 01
type: execute
status: ready-standalone-capability-gate
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_phase31_live_capabilities.py
  - frontend/e2e/phase31-live-webrtc-authority.spec.ts
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md
autonomous: false
requirements: [R2-TEXT, R2-WS, R2-WEBRTC, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry, Voice Live, and Playwright Chromium
    why: Non-mutating live capability proof against the configured exact Session pin
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Blocking preflight proves every required identifier/config/credential source/browser capability without writing raw secrets"
    - "Text and Voice Live behavior plus a correlated successful MCP call named exactly knowledge_base_retrieve are proven against the exact pin"
    - "Exact Agent definition/tools and Agent-version inventory fingerprints are identical before and after all probes"
    - "Session WebRTC remains FAIL-CLOSED unless a separate live Playwright hostile-data-channel probe on the actual Unified Training route proves exclusive backend authority"
    - "Only this capability plan is executable; production plans are regenerated after its approved verdict"
  artifacts:
    - path: backend/tests/integration/test_phase31_live_capabilities.py
      provides: Sanitized preflight, immutable resource fingerprints, no-write guards, text and Voice Live probes
    - path: frontend/e2e/phase31-live-webrtc-authority.spec.ts
      provides: Separate live browser WebRTC hostile-bypass probe on actual Unified Training path
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
      provides: Deterministic transport verdicts and regeneration inputs
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md
      provides: Gate summary explicitly blocking/regenerating Plans 31-02+
  key_links:
    - from: backend/tests/integration/test_phase31_live_capabilities.py
      to: configured CoachingSession pin and ai-coach-demo Foundry project
      via: exact read/session operations with pre/post fingerprints
      pattern: "knowledge_base_retrieve|response\\.mcp_call"
    - from: frontend/e2e/phase31-live-webrtc-authority.spec.ts
      to: frontend/src/pages/user/unified-session.tsx
      via: actual Unified Training route and hostile WebRTC data-channel event
      pattern: "response.create"
---

<objective>
Execute the sole Phase 31 capability gate without production/schema/resource mutation.

Purpose: Replace hidden environment assumptions and SDK-surface inference with sanitized live evidence for exact-Agent temporary context, native IQ invocation, Voice Live response control, and browser-level WebRTC authority.
Output: Capability probes, deterministic PASS/BLOCKED/FAIL-CLOSED verdicts, immutable Agent fingerprints, evidence, and a summary that triggers regeneration of all later plans.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CONTEXT.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-RESEARCH.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-RESEARCH-ADDENDUM.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-VALIDATION.md
@backend/tests/integration/test_unified_training_foundry_kb.py
@backend/app/services/agent_chat_service.py
@backend/app/services/voice_live_websocket.py
@backend/app/services/voice_live_webrtc.py
@frontend/src/pages/user/unified-session.tsx
@frontend/e2e/unified-training-pinned-agent.spec.ts

<interfaces>
Production files are read-only in this plan. Existing DB configuration services and Azure `DefaultAzureCredential`/CLI identity may be consumed. No raw secret is accepted into evidence or committed files. `chat_with_agent()` and all production contracts remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Build blocking sanitized preflight and immutable-resource guards</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md</files>
  <behavior>
    - Resolve an owned Skill-bound CoachingSession and report sanitized HCP/scenario/session IDs, exact Agent pin, Foundry endpoint/project availability, IQ question/marker presence, Voice Live endpoint/credential source/API version, avatar configuration, installed SDK versions, backend/frontend URLs, Chromium availability, media/device permissions, and test deadlines.
    - Use existing DB config services and `DefaultAzureCredential`/Azure CLI where available; never serialize keys/tokens/credential objects or tokenized URLs.
    - Before calls, read the exact Agent definition and tools, canonicalize sorted JSON without dropping definition fields, hash it, enumerate all Agent versions in stable order, and hash that inventory.
    - Static scan rejects reachable Agent create/update/delete/publish/version-create APIs; runtime wrappers make every Agent write method raise and count violations.
  </behavior>
  <action>Write probe-only helpers and tests. Pin project `ai-coach-demo` and require the Session exact pin to equal `Dr-Chen-Jun` version `5`; mismatch blocks before network responses. Give connection, first-event, response, MCP-terminal, avatar-ready, SDP/data-channel, bypass-observation, and cleanup separate named deadlines. Missing text/WS prerequisites produce `BLOCKED` and a failing capability gate; missing browser/WebRTC proof prerequisites produce deterministic `FAIL-CLOSED: PROOF HARNESS UNAVAILABLE`, never PASS/SKIP. Capture a protected-path hash baseline. Do not edit/import Agent sync writers, production code/tests, databases, or protected artifacts (D-01, D-04, D-14, D-17).</action>
  <verify>
    <automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py --collect-only -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\ruff.exe check tests/integration/test_phase31_live_capabilities.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\ruff.exe format --check tests/integration/test_phase31_live_capabilities.py</automated>
  </verify>
  <done>Preflight has no hidden assumptions, diagnostics are sanitized, every probe has explicit deadlines, immutable baseline fingerprints exist, and static/runtime Agent write guards are armed before live operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Probe text and Voice Live with correlated actual IQ invocation</name>
  <files>backend/tests/integration/test_phase31_live_capabilities.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md</files>
  <behavior>
    - Text initial and continuation calls send distinct temporary top-level instructions with exact `agent_reference`; continuation carries `previous_response_id`; neither call supplies tools/tool_choice.
    - Voice WS configures typed VAD with `create_response=False`, commits typed text input and authoritative final audio transcript variants, creates exactly one backend response through the typed SDK response resource with response-level temporary context, and records voice/avatar event timelines.
    - Actual IQ proof requires a successful correlated MCP call whose name is exactly `knowledge_base_retrieve`; configured tool inventory or marker answer alone fails.
    - Accepted event parsing enumerates observed concrete `response.mcp_call.*` event types, rejects failed/uncorrelated calls, and separately records whether final Responses output exposed a `type == 'mcp_call'` item.
  </behavior>
  <action>Implement non-mutating text and WS/avatar probes using ephemeral response/session operations only. Parse stream event type, response ID, call ID, exact call name, status, and terminal success. Accept final output-item proof only when the SDK exposes a correlated `mcp_call`; otherwise state `final_output_mcp_exposed: false` and require stream lifecycle proof. Require behavior and successful IQ invocation in the same chain. For Voice Live record ordered typed events: no-auto-response configuration, committed input/final transcript, backend response create, response created, MCP lifecycle, assistant transcript/output, terminal response. A 2xx, answer marker, or tool definition is never sufficient (D-03, D-04, D-13).</action>
  <verify>
    <automated>Set-Location backend; $PSNativeCommandUseErrorActionPreference=$true; $log='phase31-live-capabilities.log'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_live_capabilities.py -m integration -vv -rs 2>&1 | Tee-Object -FilePath $log; $code=$LASTEXITCODE; if ($code -ne 0) { exit $code }; if (Select-String $log -Pattern 'SKIPPED|skipped' -Quiet) { exit 1 }</automated>
  </verify>
  <done>Text and WS/avatar verdicts contain exact-pin behavioral proof, typed timelines, correlated successful `knowledge_base_retrieve` invocation, no tools override, no skips, and sanitized response/call IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the separate live browser WebRTC authority gate and finalize immutable verdicts</name>
  <files>frontend/e2e/phase31-live-webrtc-authority.spec.ts, backend/tests/integration/test_phase31_live_capabilities.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md</files>
  <behavior>
    - Playwright launches real Chromium on the actual Unified Training Session route with the preflight Session/HCP/scenario and configured media permissions/device support.
    - The probe observes the real negotiated data channel and actively sends a hostile bare `response.create`; it distinguishes authorized and injected response IDs/events.
    - `PROVEN` requires service/application rejection or enforceable prevention, exactly one authorized context-guided response, and unambiguous event routing; all other outcomes are `FAIL-CLOSED`.
    - After every probe, exact Agent definition/tool hash and version-inventory fingerprint equal baseline, runtime write count is zero, static guard remains clean, and protected hashes match.
  </behavior>
  <action>Create a separate opt-in live Playwright spec; do not represent backend pytest as browser bypass proof. Use the same Unified Training path/component that release E2E will exercise, not a playground-only or synthetic page. If current non-mutating surfaces cannot provide a production-equivalent backend-authority harness, do not invent or modify production topology: record `FAIL-CLOSED: PROOF HARNESS UNAVAILABLE`. If injection succeeds, creates an extra response, or event ownership is ambiguous, record `FAIL-CLOSED: BYPASS POSSIBLE/AMBIGUOUS`. Only a true exclusive-authority result is `PROVEN`; include observed endpoint/signaling/control/data-channel/lifecycle details required to regenerate one concrete WebRTC plan. Re-read and compare exact Agent/tool/inventory fingerprints and protected hashes. Write the summary stating Plans 31-02+ must now be regenerated; do not implement, commit, or push production work (D-11, D-15–D-17).</action>
  <verify>
    <automated>Set-Location frontend; $env:PHASE31_LIVE_WEBRTC='1'; npx playwright test --config=e2e/playwright.config.ts phase31-live-webrtc-authority.spec.ts --project=chromium --reporter=line; $browserCode=$LASTEXITCODE; if ($browserCode -ne 0) { exit $browserCode }; Set-Location ..; $e='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md'; $s='.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-01-SUMMARY.md'; $c=Get-Content $e -Raw; if ($c -notmatch 'WebRTC verdict: (PROVEN|FAIL-CLOSED)' -or $c -notmatch 'Agent resource writes: 0' -or $c -notmatch 'Definition/tool fingerprint: MATCH' -or $c -notmatch 'Version inventory fingerprint: MATCH') { exit 1 }; if ((Get-Content $s -Raw) -notmatch '31-02\+.*regenerat') { exit 1 }</automated>
  </verify>
  <done>Evidence contains one actual WebRTC verdict, no conditional executable branch, exact before/after mutation proof, and a summary that blocks all production work until plans are regenerated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Standalone non-mutating capability evidence and deterministic text, WS/avatar, and WebRTC verdicts.</what-built>
  <how-to-verify>Review 31-CAPABILITY-EVIDENCE.md and 31-01-SUMMARY.md. Confirm sanitized preflight, exact Session pin, behavior plus correlated successful `knowledge_base_retrieve`, typed Voice Live timeline, actual Unified Training browser probe or explicit proof-harness-unavailable fail-closed result, exact before/after Agent/tool/version fingerprints, zero writes, and protected hash equality. Do not approve implementation; approval authorizes regeneration of Plans 31-02+ only.</how-to-verify>
  <resume-signal>Type "capability evidence approved; regenerate Phase 31 plans" and identify the recorded WebRTC verdict.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Control |
|---|---|
| Local config/identity → live probes | Blocking sanitized preflight; no raw secret persistence |
| Test process → Foundry/Voice Live | Exact Session pin, ephemeral response/session operations, static/runtime write guards |
| Browser data channel → Voice Live | Active hostile Playwright bypass attempt; fail-closed default |
| Live events → evidence | Correlated IDs, exact event names, redaction, bounded timelines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-01-01 | Spoofing | Session/Agent target | mitigate | Resolve owned Session and assert ai-coach-demo / Dr-Chen-Jun / 5 before calls |
| T-31-01-02 | Tampering | Agent definition/version | mitigate | Exact before/after definition/tool and inventory fingerprints plus write traps |
| T-31-01-03 | Repudiation | IQ claim | mitigate | Successful correlated MCP lifecycle naming exactly knowledge_base_retrieve |
| T-31-01-04 | Information Disclosure | Preflight/evidence/logs | mitigate | Presence/source diagnostics only; redact credentials, URLs, policy, and Skill text |
| T-31-01-05 | Elevation of Privilege | WebRTC browser channel | mitigate | Real hostile-browser probe; any ambiguity remains fail-closed |
| T-31-01-06 | Denial of Service | Live event waits | mitigate | Probe-specific deadlines and deterministic cleanup |
</threat_model>

<verification>Follow 31-VALIDATION.md Wave 0. Required text/WS probes have zero skip/fail, WebRTC has one deterministic verdict, fingerprints/protected hashes match, and the human checkpoint authorizes replanning only.</verification>
<success_criteria>Capability evidence is sufficient to regenerate concrete production plans without assumptions; no production/schema/resource mutation occurred and Session WebRTC remains fail-closed unless truly browser-proven.</success_criteria>
<output>Write `31-CAPABILITY-EVIDENCE.md` and `31-01-SUMMARY.md`. Do not commit or push. Remove no fail-closed restriction until a new checked plan set is generated.</output>
