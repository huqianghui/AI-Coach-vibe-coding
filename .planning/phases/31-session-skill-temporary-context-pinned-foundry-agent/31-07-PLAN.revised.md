---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 07
type: execute
wave: 7
depends_on: [31-06R]
files_modified:
  - frontend/src/types/session.ts
  - frontend/src/api/sessions.ts
  - frontend/src/pages/user/unified-session.tsx
  - frontend/src/pages/user/unified-session.test.tsx
  - frontend/src/api/sessions.test.ts
  - frontend/public/locales/en-US/voice.json
  - frontend/public/locales/zh-CN/voice.json
  - frontend/e2e/unified-training-session-skill-context.spec.ts
autonomous: true
requirements: [R2-TEXT, R2-WS, R2-WEBRTC, R2-TRUST]
must_haves:
  truths:
    - "Unified Training sends only user text and Session URL identity for Requirement 2"
    - "Voice/avatar/WebRTC unavailable errors are shown without automatic or manual hidden fallback"
    - "Browser never supplies Foundry Conversation ID, developer context, Agent pin, Skill/SOP, tools, instructions, or continuation authority"
    - "Playwright proves the primary two-turn text story and fail-closed unavailable transports"
  artifacts:
    - path: frontend/e2e/unified-training-session-skill-context.spec.ts
      provides: Requirement 2 primary browser story and trust-boundary network audit
    - path: frontend/src/pages/user/unified-session.tsx
      provides: Text-only proven behavior plus fail-closed mode UI
  key_links:
    - from: frontend/src/pages/user/unified-session.tsx
      to: /api/v1/sessions/{session_id}/message
      via: message-only SSE request
      pattern: "message"
---

<objective>Align the browser with the server-owned text contract, remove fallback behavior, and add the mandatory primary Playwright story.</objective>

<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PLANNING-STATUS.revised.md
@frontend/src/pages/user/unified-session.tsx
@frontend/e2e/unified-training-pinned-agent.spec.ts

<decision_coverage>
D-02: Tasks 1-2 full; D-10/D-11 corrected fail-closed: Tasks 1-2 full; D-18 changed frontend coverage in Task 1 and full threshold in 31-08; D-17 all full.
</decision_coverage>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce the minimal browser contract and fail-closed UX</name>
  <files>frontend/src/types/session.ts, frontend/src/api/sessions.ts, frontend/src/pages/user/unified-session.tsx, frontend/src/pages/user/unified-session.test.tsx, frontend/src/api/sessions.test.ts, frontend/public/locales/en-US/voice.json, frontend/public/locales/zh-CN/voice.json</files>
  <behavior>
    - Text request serializes exactly `{message}`; no type/options expose server authority fields.
    - Session Voice/avatar/WebRTC capability errors render localized unavailable state and keep requested mode visible; no `setCurrentMode()` or lifecycle callback selects text/voice/model/another transport.
    - Start/send controls cannot invoke blocked transport hooks after unavailable result.
    - Text two-turn UI remains functional; standalone admin HCP test chat is unaffected.
    - Unit tests execute every new/changed branch and contribute 100% changed TS/TSX coverage.
  </behavior>
  <action>Remove Unified Session avatar-to-voice and transport fallback branches for training Sessions, not standalone playground components. Keep a clear user action to leave/return, not to silently continue on another transport. Add bilingual strings. Tighten session API types so Conversation/context/pin/tool/continuation fields cannot be represented. Write tests before implementation and cover success/error/retry/blocked modes.</action>
  <verify><automated>Set-Location frontend; npm run test -- src/api/sessions.test.ts src/pages/user/unified-session.test.tsx --coverage --coverage.include=src/api/sessions.ts --coverage.include=src/pages/user/unified-session.tsx; npx tsc -b</automated></verify>
  <done>Focused Vitest proves minimal requests, all fail-closed branches, no fallback, standalone preservation, and 100% changed-code execution.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add Requirement 2 Playwright primary user story</name>
  <files>frontend/e2e/unified-training-session-skill-context.spec.ts</files>
  <behavior>
    - Actual Unified Training route starts a Skill-bound text Session; two turns display step-N then step-N+1-guided server output.
    - Network request bodies contain only message; response UX preserves text/key-message/hint/done.
    - Browser/network audit rejects Foundry Conversation ID, developer context, Agent identity/version, Skill/SOP, tools/tool_choice, instructions, previous_response_id, and client idempotency/continuation authority.
    - Voice, avatar, and WebRTC scenarios display their exact structured unavailable state, make zero transport fallback requests, and remain on the requested mode.
    - Spec has nonzero passed, zero failed, zero skipped; mocked browser contract is not presented as live Foundry proof.
  </behavior>
  <action>Create a separate Phase 31 spec and do not edit Phase 30 acceptance evidence/spec semantics. Use route/WS interception to prove browser contract and UI only. Assert no request to Voice/token/WebRTC endpoints after the unavailable response and no mode-switch request. Cover Chinese-visible text where the configured locale requires it.</action>
  <verify><automated>Set-Location frontend; npx playwright test --config=e2e/playwright.config.ts unified-training-session-skill-context.spec.ts --project=chromium --reporter=line</automated></verify>
  <done>The mandatory primary text story and every fail-closed transport story pass with a network-level non-authority audit.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Browser → Session APIs | Minimal types plus network assertions |
| Capability error → UI | No fallback branch; explicit unavailable state |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-07-01 | Tampering | Browser request | mitigate | Message-only serialization and forbidden-key E2E audit |
| T-31-07-02 | Information Disclosure | Server context | mitigate | No frontend types/state for internal fields |
| T-31-07-03 | Elevation of Privilege | Fallback | mitigate | Remove mode-switch branches and assert zero requests |
</threat_model>

<verification>Focused Vitest coverage, TypeScript, and Phase 31 Playwright spec pass before global coverage closure.</verification>
<success_criteria>The browser is non-authoritative, text works, and blocked transports fail closed without fallback.</success_criteria>
<output>Create `31-07-SUMMARY.revised.md`; do not commit or push.</output>
