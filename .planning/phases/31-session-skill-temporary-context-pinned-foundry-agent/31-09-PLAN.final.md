---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 09
type: execute
wave: 7
depends_on: [31-08-PLAN.final.md]
files_modified:
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.final.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PRE-RELEASE-RECEIPT.final.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-POST-PUSH-RECEIPT.final.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-09-SUMMARY.final.md
autonomous: true
requirements: [R2-TEXT, R2-SOP, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry
    why: Production-path A/B and native IQ live acceptance
    env_vars:
      - name: UNIFIED_TRAINING_KB_QUESTION
        source: secure operator environment; never persist raw value
      - name: UNIFIED_TRAINING_KB_EXPECTED_MARKER
        source: secure operator environment; never persist raw value
  - service: PostgreSQL
    why: Required real-engine migration/concurrency recheck
    env_vars:
      - name: PHASE31_POSTGRES_URL
        source: disposable real PostgreSQL instance
must_haves:
  truths:
    - "Production path proves two highest-precedence directives and two independently correlated knowledge_base_retrieve calls"
    - "All offline, PostgreSQL, coverage, Playwright, protected, and forbidden gates pass with zero live skips"
    - "Staging equals an exact allowlist and exactly one commit is created"
    - "Exactly one push attempt is receipted; success proves remote SHA and leaves an honest uncommitted post-push receipt"
  artifacts:
    - path: backend/tests/integration/test_phase31_production_text_acceptance.py
      provides: Strict production-path live A/B and native IQ proof
    - path: scripts/phase31_integrity_gate.py
      provides: Protected hashes, forbidden sweep, zero-skip and allowlist checks
    - path: scripts/phase31_release_gate.py
      provides: One-commit/one-push release transaction and receipts
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-POST-PUSH-RECEIPT.final.md
      provides: Truthful post-push evidence intentionally uncommitted
  key_links:
    - from: backend/tests/integration/test_phase31_production_text_acceptance.py
      to: backend/app/services/session_turn_orchestrator.py
      via: real application Session production path
      pattern: "session_turn_orchestrator"
---

<objective>Perform strict production-path acceptance and all integrity gates, then create the sole allowlisted Requirement 2 commit and make exactly one push attempt.</objective>

<execution_context>@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-EXECUTION-STATUS.final.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-VALIDATION.final.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-COVERAGE-EVIDENCE.final.md
@CLAUDE.md</execution_context>

<context>@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@backend/tests/integration/test_phase31_live_capabilities.py
@backend/app/services/session_turn_orchestrator.py
@frontend/e2e/unified-training-session-skill-context.spec.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove production A/B, two native IQ calls, durable winner/audit, and cleanup</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.final.md</files>
  <behavior>
    - Blocking sanitized preflight resolves exact project `ai-coach-demo`, Session pin `Dr-Chen-Jun` version `5`, HCP/scenario/SkillVersion, endpoint/credential source, installed SDK signatures, IQ inputs, and cleanup support. Missing prerequisite fails; no skip.
    - Disposable application Session is created through production Session service and snapshots exact SOP. Turn A contains a stale/conflicting immutable focus reference but a final current-step directive for step N; Turn B ends with revision N+1/step N+1. Observable behavior must follow each final directive.
    - Both turns traverse production orchestrator/outbox/Conversation, and each response independently records a successful terminal MCP call named exactly `knowledge_base_retrieve`, with distinct call ID correlated to its own response ID.
    - DB proves one Conversation, immutable attempts, exactly one winning attempt/audit/assistant per turn, exact pin/digests/revisions, and monotonic progression. Injected duplicate terminal/late response cannot commit.
    - No top-level instructions, Response-input developer/system, `previous_response_id`, tool override, Agent write, mutable-latest/generic/model/transport fallback, or browser authority appears.
    - End Session, confirm cleanup closed/deleted or fail. Agent definition/tool/version fingerprints and protected hashes remain identical. Voice/avatar/WebRTC production gates return fail-closed before external calls.
  </behavior>
  <action>Execute the already authored and covered Plan 08 harness with strict opt-in and guaranteed `finally` cleanup. Record only sanitized IDs/digests/event types/call IDs. Never persist prompts, IQ values, credentials, raw URLs, or tokens. Provider cleanup failure fails acceptance.</action>
  <verify><automated>Set-Location backend; $env:PHASE31_PRODUCTION_ACCEPTANCE='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_production_text_acceptance.py -m integration -vv -rs --maxfail=1 --junitxml=phase31-live-junit.xml; $code=$LASTEXITCODE; if ($code -ne 0) { exit $code }</automated></verify>
  <done>Zero-skip live evidence proves A/B final-directive behavior, two exact correlated IQ calls, one committed winner per turn, exact pin/no-write/no-fallback, and confirmed cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Automate protected hashes, forbidden sweep, zero-skip, and exact staging preconditions</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.final.md, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PRE-RELEASE-RECEIPT.final.md</files>
  <behavior>
    - Integrity script records branch, baseline HEAD/origin SHA/commit count/status and SHA-256+size manifests for `.planning/debug/**`, `backend/storage/db-backups/**`, DB files/sidecars, Phase 30 acceptance/summary, coverage configs, and exact final-plan files.
    - Forbidden AST/text sweep rejects Agent create/update/delete/publish/version writes; mutable latest lookup; generic/model/transport fallback; browser Conversation/context/pin/tools/instructions/continuation/idempotency; top-level instructions/Response-input developer/system; `previous_response_id` in Session Conversation path; bare Session response-create; broad Git add/clean/reset/stash; protected staging.
    - Parses JUnit/Playwright JSON reports and fails unless required live/primary suites have nonzero passed, zero failed, zero skipped. Grepping console text alone is insufficient.
    - Generates exact allowlist from reviewed final diff, with each path and expected status/hash. Rejects coverage/log/temp/database/protected/Phase 30 evidence. Recomputed manifests must match before staging.
    - Pre-release receipt contains all gate commands/exit codes/digests, migration engine/head/data results, coverage percentages, changed branch reports, E2E/live counts, protected equality, allowlist, and baseline count before commit.
  </behavior>
  <action>Use the already implemented, self-tested, changed-branch-covered scripts and reviewed manifests from Plan 08. Run all gates, including Task 1 exactly once. Emit separate JUnit XML for SQLite migration, PostgreSQL migration, full backend, and production live acceptance plus Playwright JSON. Pass every report path to the integrity gate, which machine-parses nonzero passed, zero failed, zero errors, and zero skipped before generating and freezing the final pre-release allowlist/receipt. Stop on first failure; no waiver.</action>
  <verify><automated>$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; Set-Location backend; .\.venv\Scripts\ruff.exe check .; .\.venv\Scripts\ruff.exe format --check .; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_migration_sqlite.py -vv --junitxml=phase31-sqlite-junit.xml; $env:PHASE31_POSTGRES_REQUIRED='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_migration_postgresql.py -m integration -vv -rs --junitxml=phase31-postgresql-junit.xml; .\.venv\Scripts\python.exe -m pytest tests ..\scripts\tests -v --junitxml=phase31-backend-junit.xml --cov=app --cov=alembic --cov=..\scripts --cov-branch --cov-report=json:coverage.json --cov-report=xml:coverage.xml; .\.venv\Scripts\python.exe ..\scripts\verify_python_changed_branches.py verify --coverage-json coverage.json --source-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-CHANGED-PYTHON-SOURCE.txt --test-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-CHANGED-PYTHON-TESTS.txt --baseline $env:PHASE31_BASELINE_SHA --junit phase31-backend-junit.xml; Set-Location ..\frontend; npm run test:coverage -- --coverage.reporter=json --coverage.reporter=lcov --coverage.reporter=text --reporter=json --outputFile=phase31-vitest.json; npx vitest run scripts/verify-changed-v8-branches.test.mjs --reporter=json --outputFile=phase31-verifier-vitest.json; npx tsc -b; npm run build; $env:PLAYWRIGHT_JSON_OUTPUT_NAME='phase31-playwright.json'; npx playwright test --config=e2e/playwright.config.ts --project=chromium --reporter=json; node scripts/verify-changed-v8-branches.mjs verify --coverage-json coverage/coverage-final.json --source-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-CHANGED-FRONTEND-SOURCE.txt --test-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-CHANGED-FRONTEND-TESTS.txt --baseline $env:PHASE31_BASELINE_SHA --vitest-json phase31-vitest.json --vitest-json phase31-verifier-vitest.json --playwright-json phase31-playwright.json; Set-Location ..; .\backend\.venv\Scripts\python.exe scripts\phase31_integrity_gate.py verify --baseline $env:PHASE31_BASELINE_SHA --junit backend\phase31-sqlite-junit.xml --junit backend\phase31-postgresql-junit.xml --junit backend\phase31-backend-junit.xml --junit backend\phase31-live-junit.xml --playwright-json frontend\phase31-playwright.json --require-zero-skips --require-protected-equal --require-forbidden-clean --freeze-allowlist; git diff --check</automated></verify>
  <done>Automated evidence proves all gates, both DB engines, 100% changed statements/branches, frontend >=82 branches, zero live/E2E skips, protected equality, forbidden-clean status, and exact allowlist before staging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create one allowlisted commit, make one push attempt, and write honest receipt</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-POST-PUSH-RECEIPT.final.md, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-09-SUMMARY.final.md</files>
  <behavior>
    - Release script refuses if pre-release receipt/gates are absent, worktree diff differs from allowlist, protected hashes differ, baseline commit count changed, branch/upstream mismatch exists, or a push-attempt receipt already exists.
    - Stage each allowlisted path with `git add -- <exact path>` only. Cached names/status equal allowlist exactly; inspect cached diff, hashes, `git diff --cached --check`, and forbidden sweep over cached content.
    - Create exactly one commit `feat: apply Session Skill context through Foundry Conversation`; verify `baseline..HEAD` count is exactly one and commit paths equal allowlist.
    - Execute exactly one `git push origin <current-approved-branch>` command. Before execution atomically write attempt ID/timestamp/branch/local SHA/command digest; after execution append exit code and sanitized stdout/stderr digest. Never retry.
    - On success, fetch/resolve remote and require remote SHA == local. On failure, record failure and stop without another commit/push/branch.
    - Recompute protected hashes. Write only the two paths listed in the frozen `31-POST-FREEZE-EVIDENCE-ALLOWLIST.txt`; assert every other worktree path/hash still equals the freeze and both evidence files are uncommitted/not staged. Never amend or create a second commit/push.
  </behavior>
  <action>Invoke the already tested Plan 08 `release-once` subcommand exactly once; it performs exact staging, commit, atomic push-attempt ledger, one push command, remote verification, and allowlisted evidence writing. Then invoke the read-only `verify-receipt` subcommand separately. No executable source or staging allowlist may be created or modified after Task 2 freezes the pre-release manifest.</action>
  <verify><automated>.\backend\.venv\Scripts\python.exe scripts\phase31_release_gate.py release-once --baseline $env:PHASE31_BASELINE_SHA --commit-message "feat: apply Session Skill context through Foundry Conversation" --post-freeze-allowlist .planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-POST-FREEZE-EVIDENCE-ALLOWLIST.txt; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\backend\.venv\Scripts\python.exe scripts\phase31_release_gate.py verify-receipt --baseline $env:PHASE31_BASELINE_SHA --expect-commits 1 --expect-push-attempts 1 --require-remote-equals-local --require-post-receipt-uncommitted --require-protected-equal --post-freeze-allowlist .planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-POST-FREEZE-EVIDENCE-ALLOWLIST.txt</automated></verify>
  <done>Exactly one allowlisted commit and one receipted push attempt exist; on success remote equals local, protected files match, and post-push evidence is truthfully uncommitted.</done>
</task>
</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Fabricated live/IQ success | mitigate | zero-skip parsed reports and response/call correlations |
| Protected/unrelated staging | mitigate | automated manifests plus exact cached allowlist |
| Multiple commit/push attempts | mitigate | baseline count and atomic push-attempt ledger/refusal |
| Receipt falsely claimed committed | mitigate | post-push generation and explicit unstaged/uncommitted check |
</threat_model>

<verification>No staging occurs before all gates. Final verifier proves one commit, one push attempt, remote equality on success, protected equality, and uncommitted post-push receipt.</verification>
<success_criteria>Requirement 2 is live-proven and released exactly once without overstating provider semantics or release evidence.</success_criteria>
<output>Create only the listed new final evidence/summary files. Do not amend and do not push a second time.</output>
