---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 05
type: tdd
wave: 5
depends_on: [31-04R]
files_modified:
  - backend/app/services/foundry_conversation_service.py
  - backend/app/services/session_turn_orchestrator.py
  - backend/app/services/agent_chat_service.py
  - backend/app/services/session_service.py
  - backend/tests/test_foundry_conversation_service.py
  - backend/tests/test_session_turn_orchestrator.py
  - backend/tests/test_agent_chat_service.py
autonomous: true
requirements: [R2-TEXT, R2-SOP, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry
    why: Production text and cleanup use configured Responses/Conversations APIs
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Each Skill-bound Session owns one lazily created durable Foundry Conversation"
    - "Each turn appends one latest-revision developer item immediately before its user item and uses exact agent_reference"
    - "Explicit Conversation is the only continuation mechanism; no instructions, previous_response_id, tools, or tool_choice is sent"
    - "Successful output, IQ correlations, immutable audit, and next-step progression finalize in order"
    - "Conversation cleanup is durable, retryable, and never replaced by fallback creation"
  artifacts:
    - path: backend/app/services/foundry_conversation_service.py
      provides: Conversation provision/append/respond/delete lifecycle
    - path: backend/app/services/session_turn_orchestrator.py
      provides: Exact ordered turn transaction and immutable audit
  key_links:
    - from: backend/app/services/session_turn_orchestrator.py
      to: backend/app/services/foundry_conversation_service.py
      via: developer item then user item then exact-Agent Response
      pattern: "append_developer.*append_user.*create_response"
---

<objective>Integrate the only proven temporary-context surface into exact-Agent Session text while preserving standalone Phase 30/admin chat behavior.</objective>

<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PLANNING-STATUS.revised.md
@backend/app/services/agent_chat_service.py
@backend/app/api/sessions.py

<interfaces>
The proven SDK sequence is: server creates a Conversation; adds a typed `developer` message item; adds/sends a typed user item; calls Responses with that Conversation and exact `extra_body.agent_reference`. Top-level `instructions` is rejected. Explicit Conversation replaces `previous_response_id` for this path.
</interfaces>
<decision_coverage>
D-01–D-04: Tasks 1-2 full; corrected D-08: Tasks 1-2 full; D-12/D-13: Task 2 full; D-16 deferred to 31-09; D-17 all full.
</decision_coverage>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement durable Foundry Conversation lifecycle</name>
  <files>backend/app/services/foundry_conversation_service.py, backend/app/services/session_service.py, backend/tests/test_foundry_conversation_service.py</files>
  <behavior>
    - First Skill-bound text turn claims a durable provisioning lease, creates exactly one Conversation, persists ID/state, and releases lease.
    - Retry of a persisted active mapping reuses it; provisioning conflicts wait/busy; unknown outcome becomes failed/manual cleanup, never blind replacement.
    - Each turn appends a developer item with current revision, then a user item; stale directives remain history but latest explicitly supersedes all earlier context revisions.
    - End/delete/expiry blocks new turns, deletes the mapped Conversation, clears raw ID only after confirmed deletion, and records closed timestamp.
    - Transient delete failure records sanitized `cleanup_pending`; bounded startup/maintenance retry uses backoff and cannot create a replacement.
    - SDK/service contract tests prove Conversation calls and reject `previous_response_id`, top-level `instructions`, `tools`, and `tool_choice`.
  </behavior>
  <action>Build a typed async facade around the existing configured OpenAI client, moving blocking SDK calls through `asyncio.to_thread`. Create lazily on first text turn. Use persisted leases and deterministic idempotency metadata/headers only where the installed SDK/service explicitly supports them; tests must introspect installed signatures and fail if the assumed contract is absent. Add bounded timeouts and sanitized errors. Wire cleanup into Session end/deletion and a callable startup maintenance sweep without editing unrelated retention behavior.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_foundry_conversation_service.py -vv --cov=app.services.foundry_conversation_service --cov-report=term-missing --cov-fail-under=100</automated></verify>
  <done>Provision, reuse, ordering, unknown-outcome, cleanup, retry, retention, and SDK keyword contracts are 100% covered with no fallback.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Orchestrate exact-Agent responses, immutable audit, and next-turn commit</name>
  <files>backend/app/services/session_turn_orchestrator.py, backend/app/services/agent_chat_service.py, backend/tests/test_session_turn_orchestrator.py, backend/tests/test_agent_chat_service.py</files>
  <behavior>
    - Skill-bound Session path freezes envelope N, appends developer N then user, and streams Response on the mapped Conversation with exact Session name/version.
    - No request contains top-level instructions, previous_response_id, tools/tool_choice, HCP-latest identity, generic adapter, model fallback, or Agent write.
    - MCP event parsing correlates exact successful `knowledge_base_retrieve` call IDs to the same response; sanitized metadata is audit-only and does not replace success behavior.
    - On terminal success: persist assistant and response data, insert one immutable audit row, then commit monotonic next step. Failure writes a terminal failure audit and does not advance.
    - Duplicate stream terminal/MCP events and retry of unfinished turn are idempotent; concurrent turn remains blocked.
    - Standalone `chat_with_agent()` and non-Skill Phase 30 `stream_agent_response()` retain their existing `previous_response_id` contract and behavior.
  </behavior>
  <action>Create a separate required-context Session API; do not globally alter standalone callers. Extend stream event types only as necessary for sanitized MCP lifecycle evidence. The orchestrator owns transaction order and uses the Plan 04 claim/finalization contract. Insert each audit once at terminal outcome; never update/delete it. Ensure cancellation closes streams and leaves the retryable pending turn authoritative.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_turn_orchestrator.py tests/test_agent_chat_service.py -vv --cov=app.services.session_turn_orchestrator --cov=app.services.agent_chat_service --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/session_turn_orchestrator.py,app/services/foundry_conversation_service.py'; .\.venv\Scripts\ruff.exe check app/services/foundry_conversation_service.py app/services/session_turn_orchestrator.py app/services/agent_chat_service.py app/services/session_service.py tests/test_foundry_conversation_service.py tests/test_session_turn_orchestrator.py tests/test_agent_chat_service.py; .\.venv\Scripts\ruff.exe format --check app/services/foundry_conversation_service.py app/services/session_turn_orchestrator.py app/services/agent_chat_service.py app/services/session_service.py tests/test_foundry_conversation_service.py tests/test_session_turn_orchestrator.py tests/test_agent_chat_service.py</automated></verify>
  <done>Two-turn, retry, cancellation, failure, MCP correlation, audit, and standalone/non-Skill regression tests pass with 100% changed-code coverage.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Session DB → Foundry | Exact pin, mapped Conversation, canonical developer item |
| Foundry stream → audit | Response/call correlation and sanitized terminal metadata |
| Lifecycle retry → Foundry | Durable state/lease; no blind replacement |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-05-01 | Spoofing | Agent identity | mitigate | Session exact pin only |
| T-31-05-02 | Tampering | Context/continuation | mitigate | Server Conversation and adjacent latest developer item |
| T-31-05-03 | Repudiation | IQ/context application | mitigate | Immutable correlated audit |
| T-31-05-04 | Denial of Service | Orphan Conversation | mitigate | Durable cleanup_pending sweep/backoff |
| T-31-05-05 | Elevation of Privilege | Agent tools | mitigate | No tools override or Agent writes; request-shape tests |
</threat_model>

<verification>Focused lifecycle/orchestrator/regression tests and Ruff pass; changed production services reach 100% coverage.</verification>
<success_criteria>The exact pinned Agent is behaviorally guided through the proven Conversation developer-item surface with durable lifecycle and audit.</success_criteria>
<output>Create `31-05-SUMMARY.revised.md`; do not commit or push.</output>
