---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 04
type: execute
wave: 4
depends_on: [31-03]
files_modified:
  - backend/app/services/voice_live_websocket.py
  - backend/tests/test_voice_live_websocket.py
  - backend/tests/test_voice_live_session_context.py
autonomous: true
requirements: [R2-WS, R2-SOP, R2-IQ, R2-TRUST]
must_haves:
  truths:
    - "Session-bound voice and avatar disable auto-response and use one backend-created response per committed turn"
    - "Each response receives the canonical response-level additional_instructions for exact Session pin"
    - "Browser override events are rejected by allowlist before Azure forwarding"
    - "Transcript/response completion ordering advances SOP only for the next response"
  artifacts:
    - path: backend/app/services/voice_live_websocket.py
      provides: Session-bound WS/avatar response authority and event policy
    - path: backend/tests/test_voice_live_session_context.py
      provides: Transport parity and turn-correlation tests
  key_links:
    - from: backend/app/services/voice_live_websocket.py
      to: backend/app/services/session_skill_context.py
      via: build rendering immediately before backend response.create
      pattern: "additional_instructions"
---
<objective>Make the backend the sole response authority for Session-bound Voice Live WS and avatar turns.</objective>
<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@backend/app/services/voice_live_websocket.py
@backend/app/services/session_turn_orchestrator.py
@backend/tests/test_voice_live_websocket.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce Session-bound Voice Live event and VAD policy</name>
  <files>backend/app/services/voice_live_websocket.py, backend/tests/test_voice_live_websocket.py</files>
  <behavior>
    - Session Agent config uses `AzureSemanticVad(create_response=False)` as proven live.
    - Client allowlist permits required audio append/commit/clear, interruption, and avatar SDP intents only with bounded fields.
    - Client `response.create`, instruction/tool/Agent/session override, arbitrary `session.update`, and unknown events produce structured error and are never forwarded.
    - Standalone admin/playground paths preserve their existing behavior; no broad regression rewrite.
  </behavior>
  <action>Pass an explicit `session_bound` policy into forwarding. Replace blind forwarding only for owned training Sessions with typed allowlist validation. The first frame remains exactly `session_id`; all identity/context is resolved server-side. Keep exact `connect(agent_name, agent_version, project_name)` and never select model mode for Session flows (D-01, D-02, D-08).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_voice_live_websocket.py -vv --cov=app.services.voice_live_websocket --cov-report=term-missing; .\.venv\Scripts\ruff.exe check app/services/voice_live_websocket.py tests/test_voice_live_websocket.py; .\.venv\Scripts\ruff.exe format --check app/services/voice_live_websocket.py tests/test_voice_live_websocket.py</automated></verify>
  <done>Session browser cannot create/override a response and VAD cannot auto-create one; allowed media/avatar events still function.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Create and correlate voice/avatar responses server-side</name>
  <files>backend/app/services/voice_live_websocket.py, backend/tests/test_voice_live_websocket.py, backend/tests/test_voice_live_session_context.py</files>
  <behavior>
    - Final authoritative user transcript/commit triggers exactly one backend response create with current canonical `additional_instructions`.
    - Exact Agent pin and Agent-native IQ configuration remain unchanged; no tools supplied.
    - Response/transcript events correlate by item/response/turn IDs across ordering permutations; duplicate terminal events are idempotent.
    - Successful terminal response persists assistant transcript/audit then monotonic progression; failures keep step unchanged.
    - Avatar modality uses identical authority/context logic.
  </behavior>
  <action>Use the live-proven SDK response creation surface. Add a per-connection turn correlator calling shared envelope/progression helpers. Buffer only bounded final transcript state needed for ordering; never use partial transcripts for progression. Return safe lifecycle events to browser but not raw context. Test voice and avatar, disconnect/cancel/error, and event permutations (D-03, D-04, D-06, D-08).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_voice_live_websocket.py tests/test_voice_live_session_context.py -vv --cov=app.services.voice_live_websocket --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/voice_live_websocket.py'; .\.venv\Scripts\ruff.exe check app/services/voice_live_websocket.py tests/test_voice_live_websocket.py tests/test_voice_live_session_context.py</automated></verify>
  <done>WS voice/avatar have one trusted response per turn, context parity, durable correlation, next-turn progression, and 100% changed-code coverage.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Browser WS → backend | Session-bound typed event allowlist and payload bounds |
| Backend → Voice Live | Exact pin, no auto-response, response-level context |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-04-01 | Tampering | Client events | mitigate | Allowlist; reject overrides/unknowns before send |
| T-31-04-02 | Denial of Service | Event/transcript buffers | mitigate | Size limits, one active turn, bounded timeout |
| T-31-04-03 | Tampering | Duplicate completions | mitigate | Correlated idempotent turn keys |
| T-31-04-04 | Information Disclosure | Context | mitigate | Never echo/render context to browser/logs |
</threat_model>
<verification>Focused WS/avatar tests, 100% changed-code coverage, Ruff check/format all pass.</verification>
<success_criteria>All Session-bound WS/Avatar responses are server-created with current context and exact Agent IQ intact.</success_criteria>
<output>Create `31-04-SUMMARY.md`; do not commit/push.</output>
