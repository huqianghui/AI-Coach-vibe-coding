---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 03
type: tdd
wave: 1
depends_on: [31-02-PLAN.revised.md]
files_modified:
  - backend/app/models/session.py
  - backend/app/models/session_turn.py
  - backend/app/models/session_turn_attempt.py
  - backend/app/models/session_turn_attempt_event.py
  - backend/app/models/session_turn_context_audit.py
  - backend/app/models/__init__.py
  - backend/app/schemas/session.py
  - backend/alembic/versions/{actual_new_revision}_phase31_session_turn_outbox.py
  - backend/tests/test_phase31_session_turn_models.py
  - backend/tests/test_phase31_migration_sqlite.py
  - backend/tests/integration/test_phase31_migration_postgresql.py
  - scripts/verify_python_changed_branches.py
  - scripts/tests/test_verify_python_changed_branches.py
  - frontend/scripts/verify-changed-v8-branches.mjs
  - frontend/src/scripts/verify-changed-v8-branches.test.ts
  - frontend/scripts/verify-changed-v8-branches.d.mts
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-PYTHON-SOURCE.txt
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-PYTHON-TESTS.txt
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-SOURCE.txt
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-TESTS.txt
autonomous: true
requirements: [R2-SOP, R2-TEXT, R2-TRUST]
must_haves:
  truths:
    - "Skill-bound Sessions persist immutable structured SOP authority and one internal Conversation lifecycle"
    - "A mutable durable turn/outbox is distinct from immutable provider attempts and immutable context audit"
    - "At most one application result can win even when a provider operation executes more than once"
    - "SQLite and real PostgreSQL prove upgrade, downgrade, heads, constraints, and data preservation"
  artifacts:
    - path: backend/app/models/session_turn.py
      provides: Mutable pending-turn/outbox aggregate
    - path: backend/app/models/session_turn_attempt.py
      provides: Immutable provider-attempt request-start facts
    - path: backend/app/models/session_turn_attempt_event.py
      provides: Append-only post-call outcome and classification history
    - path: backend/app/models/session_turn_context_audit.py
      provides: Immutable committed context/result audit
    - path: backend/alembic/versions/{actual_new_revision}_phase31_session_turn_outbox.py
      provides: Reversible persistence migration chained to actual head
  key_links:
    - from: backend/app/models/session_turn_attempt.py
      to: backend/app/models/session_turn.py
      via: RESTRICT foreign key and append-only sequence uniqueness
      pattern: "ondelete=\"RESTRICT\""
---

<objective>Create the durable database foundation for Session SOP snapshots, Conversation lifecycle, pending-turn/outbox orchestration, immutable attempts, and immutable committed audits.</objective>

<execution_context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-EXECUTION-STATUS.final.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-VALIDATION.final.md
@CLAUDE.md
</execution_context>

<context>
@backend/app/models/session.py
@backend/app/models/message.py
@backend/app/models/__init__.py
@backend/alembic/versions/a34a_add_session_agent_pin.py

<interfaces>
`CoachingSession` already owns exact `agent_name`, `agent_version`, `skill_id`, `skill_version_id`, immutable `focus_instruction`, and `sop_current_step`. New authority must remain internal and must not enter browser request/response schemas.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Model Session authority, mutable outbox, and immutable histories</name>
  <files>backend/app/models/session.py, backend/app/models/session_turn.py, backend/app/models/session_turn_attempt.py, backend/app/models/session_turn_attempt_event.py, backend/app/models/session_turn_context_audit.py, backend/app/models/__init__.py, backend/app/schemas/session.py, backend/tests/test_phase31_session_turn_models.py</files>
  <behavior>
    - Session stores canonical `sop_snapshot_json`/SHA-256, Conversation raw ID internally, Conversation state `unprovisioned|creating|active|create_unknown|cleanup_pending|closed`, provider create operation/idempotency ID when supported, create/delete lease token+expiry, retry counters, next cleanup time, sanitized error, timestamps, and monotonic context revision.
    - `SessionTurn` is the mutable aggregate/outbox with server UUID `turn_key`, unique `(session_id, turn_key)`, statuses `pending|leased|provider_pending|provider_unknown|reconciling|succeeded|failed_terminal|cancelled`, input digest, frozen step/revision/context digest, lease owner/expiry, attempt count, provider operation/response IDs, winning attempt ID, retry/reconcile timestamps, and sanitized error.
    - Legal compare-and-set transitions are explicit; terminal rows cannot return to pending. A completed identical message creates a new turn; retry reuses only the same unfinished server turn.
    - `SessionTurnAttempt` is an immutable request-start fact with unique `(turn_id, attempt_number)`, request digest, lease/correlation metadata known before dispatch, and created timestamp. It is inserted and committed before the provider call and is never updated or deleted.
    - `SessionTurnAttemptEvent` is append-only with unique `(attempt_id, event_sequence)` and immutable event kinds `dispatched|known_success|known_failure|timeout|unknown|reconciled_success|reconciled_failure|winner_selected|late_duplicate|cleanup_observed`. Provider response/call IDs, terminal classification, sanitized error digest, and observed timestamp are recorded only as new event rows.
    - `SessionTurnContextAudit` is append-only and written only for the committed winner/final terminal result; unique `(session_id, turn_key)`, exact pin/Skill/version, snapshot/focus/context digests, applied step/revision, message IDs, Conversation digest, winning attempt/provider response, IQ correlation, progression result, and created time.
    - Attempt/audit FKs and Session parent lifecycle use `RESTRICT`; end/expiry does not delete the parent. Explicit administrative deletion is blocked while attempts/audits/Conversation retention remain.
    - Browser schemas neither accept nor expose any internal authority.
  </behavior>
  <action>Write failing metadata, transition, uniqueness, immutability, winner, and schema-exclusion tests first. Implement separate models; do not collapse attempt history into mutable outbox or audit. Add SQLAlchemy before-update/delete guards for immutable rows and service-independent terminal-transition validation. Document that the guarantee is at-most-one committed application result; provider execution may duplicate after unknown outcomes.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_session_turn_models.py -vv --cov=app.models.session --cov=app.models.session_turn --cov=app.models.session_turn_attempt --cov=app.models.session_turn_attempt_event --cov=app.models.session_turn_context_audit --cov-branch --cov-report=term-missing --cov-fail-under=100</automated></verify>
  <done>All aggregate/outbox states, constraints, immutable rows, retry/winner/duplicate semantics, internal schema boundary, statements, and branches are 100% covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reversible migration on SQLite and real PostgreSQL</name>
  <files>backend/alembic/versions/{actual_new_revision}_phase31_session_turn_outbox.py, backend/tests/test_phase31_migration_sqlite.py, backend/tests/integration/test_phase31_migration_postgresql.py</files>
  <behavior>
    - Executor runs `alembic heads` first, substitutes a real unique revision filename/ID, and sets `down_revision` to the discovered sole head; `{actual_new_revision}` is a planning token, never the literal filename.
    - Isolated populated SQLite upgrades from previous head, retains old rows, inserts/reads all new entities, enforces uniqueness/RESTRICT, downgrades exactly one revision, verifies removal and old-data survival, then re-upgrades.
    - Real PostgreSQL performs the same cycle plus concurrent winner/lease uniqueness, partial/nullable provider-ID uniqueness behavior, FK/RESTRICT behavior, and transactional rollback data checks.
    - Both engines finish at one identical head. No workspace database or sidecar is opened, copied, deleted, or migrated.
  </behavior>
  <action>Create a single migration using batch operations only where SQLite needs them and native constraints valid on both engines. The PostgreSQL integration test requires a disposable `PHASE31_POSTGRES_URL`; missing URL is a hard failure for plan completion, not a skip. Never infer PostgreSQL compatibility from generated SQL.</action>
  <verify><automated>$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; Set-Location backend; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_migration_sqlite.py -vv; $env:PHASE31_POSTGRES_REQUIRED='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_migration_postgresql.py -m integration -vv -rs</automated></verify>
  <done>One actual migration head and complete SQLite/PostgreSQL upgrade-downgrade-data-constraint evidence pass with zero skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create deterministic changed-branch verifiers and enforce Plan 03 manifest</name>
  <files>scripts/verify_python_changed_branches.py, scripts/tests/test_verify_python_changed_branches.py, frontend/scripts/verify-changed-v8-branches.mjs, frontend/scripts/verify-changed-v8-branches.d.mts, frontend/src/scripts/verify-changed-v8-branches.test.ts, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-PYTHON-SOURCE.txt, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-PYTHON-TESTS.txt, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-SOURCE.txt, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-TESTS.txt</files>
  <behavior>
    - Both verifiers expose exactly two tested subcommands. `snapshot --baseline <sha> --output <json>` atomically writes normalized path/hash/status facts and refuses overwrite. `verify --coverage-json <json> --source-manifest <txt> --test-manifest <txt> (--baseline <sha>|--start-snapshot <json>) [--junit <xml> ...] [--vitest-json <json> ...] [--playwright-json <json> ...]` has no aliases or alternative flag names; report flags are repeatable so aggregate gates can prove separate suites.
    - Source manifests contain production/application, migration, and utility scripts and require 100% statements and branches. Test manifests contain changed pytest/Vitest/Playwright test infrastructure; the verifier requires each entry to be collected and executed in machine-readable reports with nonzero passed and zero failed/errors/skipped, instead of making a false source-coverage claim.
    - Python verifier consumes branch-aware coverage JSON and fails when a source is below 100%, absent from coverage, or any changed Python path is absent from exactly one reviewed source/test manifest. V8 verifier applies the same policy to TS/TSX.
    - Fixture tests prove snapshot creation, overwrite refusal, hash/status delta, missing file, uncovered statement/branch, malformed report, stale/extra/dual-classified path, missing test execution, skip/failure, and success for both verifiers.
    - Plan 03 source manifest includes models, schemas, migration, and Python verifier; test manifest includes every changed pytest/verifier-test module.
  </behavior>
  <action>Implement both reusable verifiers before later plans use snapshots. Plan 03 uses immutable `PHASE31_BASELINE_SHA`. Generate branch-aware coverage including `app`, the actual migration source, Python verifier, and V8 verifier; emit JUnit/Vitest JSON; run adversarial tests; then invoke each verifier with the exact grammar. This gate must pass before Plan 04.</action>
  <verify><automated>$PSNativeCommandUseErrorActionPreference=$true; $ErrorActionPreference='Stop'; Set-Location backend; $env:PHASE31_POSTGRES_REQUIRED='1'; .\.venv\Scripts\python.exe -m pytest tests/test_phase31_session_turn_models.py tests/test_phase31_migration_sqlite.py tests/integration/test_phase31_migration_postgresql.py ..\scripts\tests\test_verify_python_changed_branches.py -vv -m 'not integration or integration' --junitxml=phase31-plan03-junit.xml --cov=app.models.session --cov=app.models.session_turn --cov=app.models.session_turn_attempt --cov=app.models.session_turn_attempt_event --cov=app.models.session_turn_context_audit --cov=app.schemas.session --cov=alembic.versions.{actual_new_revision}_phase31_session_turn_outbox --cov=..\scripts\verify_python_changed_branches.py --cov-branch --cov-report=json:phase31-plan03-coverage.json; .\.venv\Scripts\python.exe ..\scripts\verify_python_changed_branches.py verify --coverage-json phase31-plan03-coverage.json --source-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-03-CHANGED-PYTHON-SOURCE.txt --test-manifest ..\.planning\phases\31-session-skill-temporary-context-pinned-foundry-agent\31-03-CHANGED-PYTHON-TESTS.txt --baseline $env:PHASE31_BASELINE_SHA --junit phase31-plan03-junit.xml; Set-Location ..\frontend; npx vitest run src/scripts/verify-changed-v8-branches.test.ts --coverage --coverage.provider=v8 --coverage.include=scripts/verify-changed-v8-branches.mjs --coverage.reporter=json --reporter=json --outputFile=phase31-plan03-vitest.json; node scripts/verify-changed-v8-branches.mjs verify --coverage-json coverage/coverage-final.json --source-manifest ../.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-SOURCE.txt --test-manifest ../.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-03-CHANGED-FRONTEND-TESTS.txt --baseline $env:PHASE31_BASELINE_SHA --vitest-json phase31-plan03-vitest.json</automated></verify>
  <done>Both verifiers are adversarially self-tested and Plan 03 has machine-enforced 100% changed statements/branches with complete manifest membership.</done>
</task>
</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Concurrent winner tampering | mitigate | DB uniqueness, CAS transitions, winning attempt FK |
| Repudiated provider duplicate | mitigate | immutable attempt per invocation and late/duplicate classification |
| Context/Conversation disclosure | mitigate | internal-only fields and schema tests |
| Parent deletion destroys audit | mitigate | RESTRICT retention and lifecycle tests |
</threat_model>

<verification>Focused tests, Ruff, actual one-head check, and both database engines pass; changed Python statements/branches are 100% covered.</verification>
<success_criteria>All later orchestration has durable, auditable, engine-proven state and makes no false exactly-once promise.</success_criteria>
<output>Create a new `31-03-SUMMARY.final.md`; do not commit or push.</output>
