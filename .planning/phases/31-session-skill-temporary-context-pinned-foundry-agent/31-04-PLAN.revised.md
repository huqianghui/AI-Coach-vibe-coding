---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 04
type: tdd
wave: 4
depends_on: [31-03R]
files_modified:
  - backend/app/services/session_skill_context.py
  - backend/app/services/session_turn_progression.py
  - backend/app/services/session_service.py
  - backend/tests/test_session_skill_context.py
  - backend/tests/test_session_turn_progression.py
  - backend/tests/test_session_service.py
autonomous: true
requirements: [R2-SOP, R2-TRUST]
must_haves:
  truths:
    - "Session creation captures structured SOP from the exact pinned Skill version and never rewrites focus_instruction"
    - "Every turn renders one canonical envelope from the immutable snapshot and committed step"
    - "Progression is monotonic and becomes visible only after successful terminal persistence"
    - "Concurrent/retried turns resolve to one server idempotency authority and historical invalid snapshots fail closed"
  artifacts:
    - path: backend/app/services/session_skill_context.py
      provides: Canonical snapshot validation, digesting, and per-turn rendering
    - path: backend/app/services/session_turn_progression.py
      provides: Typed deterministic progression decision and monotonic commit
  key_links:
    - from: backend/app/services/session_service.py
      to: backend/app/services/session_skill_context.py
      via: creation from exact scenario.skill_version_id
      pattern: "build_sop_snapshot"
---

<objective>Build the canonical server context, progression, idempotency, and concurrency services independently of Foundry transport.</objective>

<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PLANNING-STATUS.revised.md
@backend/app/services/session_service.py
@backend/app/services/skill_focus_service.py
@backend/app/models/skill.py
@backend/app/models/session.py

<decision_coverage>
D-05: Task 1 full; D-06: Tasks 1-2 full; D-07: Task 2 full; D-09: Task 2 full; D-12: Task 2 audit-input contract; D-17: all full.
</decision_coverage>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Capture and render canonical immutable Session context</name>
  <files>backend/app/services/session_skill_context.py, backend/app/services/session_service.py, backend/tests/test_session_skill_context.py, backend/tests/test_session_service.py</files>
  <behavior>
    - `create_session()` selects the exact `scenario.skill_version_id`, validates it belongs to `scenario.skill_id`, canonicalizes ordered SOP steps, snapshots the structure/digest, and preserves the original `focus_instruction` bytes.
    - Mutable latest Skill lookup is never used after Session creation.
    - `render_turn_context(session)` validates snapshot/digest/pin and renders immutable focus reference followed by a separate current-step directive.
    - The directive includes context revision, exact current step/total, and `supersedes all earlier Session-context developer items`; it contains no secrets or browser data.
    - Missing/corrupt historical snapshot raises structured `SESSION_SOP_SNAPSHOT_INVALID` before any Foundry operation.
  </behavior>
  <action>Write pure canonical JSON/digest/render tests first. Add a frozen typed snapshot/envelope API. Replace Session creation's cloud/latest Skill consumption with direct exact `SkillVersion` selection for snapshot authority; retain any non-Session standalone consumption behavior elsewhere. Do not call `compose_focus_instruction()` per turn or mutate `focus_instruction` (D-05/D-06).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_skill_context.py tests/test_session_service.py -vv --cov=app.services.session_skill_context --cov=app.services.session_service --cov-report=term-missing; .\.venv\Scripts\python.exe -m coverage report --fail-under=100 --include='app/services/session_skill_context.py'</automated></verify>
  <done>Creation and rendering tests cover exact-version authority, canonical digest, byte-preserved focus, latest-Skill mutation resistance, superseding revision text, and fail-closed history.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add typed progression and server idempotency semantics</name>
  <files>backend/app/services/session_turn_progression.py, backend/app/services/session_service.py, backend/tests/test_session_turn_progression.py, backend/tests/test_session_service.py</files>
  <behavior>
    - Preserve public `detect_sop_step()` behavior/signature; wrap it in a new typed `decide_next_session_step()` API.
    - Freeze step/revision before response. Only after assistant, response, and audit persistence can compare-and-set commit `max(current, detected)` for the next turn.
    - Detector error, invalid result, timeout, response failure, or persistence rollback keeps current step.
    - Under a per-Session transactional/lease lock, retry reuses the one unfinished server-generated idempotency key; completed same-text input starts a new turn.
    - Concurrent second turns receive a typed busy/conflict result; no duplicate context revision is allocated.
  </behavior>
  <action>Implement a typed progression result and a transaction-safe turn claim/finalization contract using persisted Session lifecycle/audit fields, not process-only locks. Use compare-and-set predicates suitable for SQLite tests and PostgreSQL production. Refactor `update_sop_progress()` so it no longer fetches latest Skill, heuristically increments, or rewrites focus; retain a compatibility wrapper only if existing callers require it, and update all callers/tests atomically (D-07/D-09).</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\python.exe -m pytest tests/test_session_turn_progression.py tests/test_session_service.py -vv --cov=app.services.session_turn_progression --cov-report=term-missing --cov-fail-under=100; .\.venv\Scripts\ruff.exe check app/services/session_skill_context.py app/services/session_turn_progression.py app/services/session_service.py tests/test_session_skill_context.py tests/test_session_turn_progression.py tests/test_session_service.py; .\.venv\Scripts\ruff.exe format --check app/services/session_skill_context.py app/services/session_turn_progression.py app/services/session_service.py tests/test_session_skill_context.py tests/test_session_turn_progression.py tests/test_session_service.py</automated></verify>
  <done>Progression, failure, retry, duplicate-event, and concurrency semantics are typed, durable, monotonic, compatible, and 100% covered.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| SkillVersion → Session snapshot | Exact FK ownership validation and canonical digest |
| Concurrent requests → Session | Durable compare-and-set claim and unique audit key |
| Detector → progression | Clamp/validate, monotonic commit only after terminal persistence |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-04-01 | Tampering | Mutable Skill | mitigate | Never fetch latest content per turn |
| T-31-04-02 | Repudiation | Retry | mitigate | Server-generated persisted idempotency key |
| T-31-04-03 | Denial of Service | Concurrent turn | mitigate | Lease timeout and typed busy response |
| T-31-04-04 | Tampering | Detector result | mitigate | Validated monotonic compare-and-set |
</threat_model>

<verification>Focused suites and Ruff pass; both new service modules have 100% line/branch changed-code coverage.</verification>
<success_criteria>One canonical immutable context and next-turn progression contract exists before any Foundry integration.</success_criteria>
<output>Create `31-04-SUMMARY.revised.md`; do not commit or push.</output>
