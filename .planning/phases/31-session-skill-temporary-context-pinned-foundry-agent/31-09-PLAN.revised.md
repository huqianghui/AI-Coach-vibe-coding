---
phase: 31-session-skill-temporary-context-pinned-foundry-agent
plan: 09
type: execute
wave: 9
depends_on: [31-08R]
files_modified:
  - backend/tests/integration/test_phase31_production_text_acceptance.py
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.revised.md
  - .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-09-SUMMARY.revised.md
autonomous: true
requirements: [R2-TEXT, R2-SOP, R2-IQ, R2-TRUST]
user_setup:
  - service: Azure AI Foundry
    why: Strict live production-path behavior plus native IQ acceptance
    env_vars:
      - name: UNIFIED_TRAINING_KB_QUESTION
        source: Existing secure operator environment; never persist value
      - name: UNIFIED_TRAINING_KB_EXPECTED_MARKER
        source: Existing secure operator environment; never persist value
    dashboard_config: []
must_haves:
  truths:
    - "Live production path demonstrates two distinct per-turn directives on the exact ai-coach-demo / Dr-Chen-Jun / 5 pin"
    - "Each live production response has its own correlated successful exact knowledge_base_retrieve call"
    - "Agent fingerprints, protected hashes, Voice/avatar/WebRTC fail-closed status, cleanup, and all offline gates pass before release"
    - "Exactly one explicit-allowlist Requirement 2 commit is created and pushed once"
  artifacts:
    - path: backend/tests/integration/test_phase31_production_text_acceptance.py
      provides: Strict no-skip live acceptance through production Session orchestration
    - path: .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.revised.md
      provides: Complete pre-commit gates, live evidence, allowlist, protected hashes, commit and post-push receipt
  key_links:
    - from: backend/tests/integration/test_phase31_production_text_acceptance.py
      to: backend/app/services/session_turn_orchestrator.py
      via: real production service path, not direct probe-only SDK calls
      pattern: "session_turn_orchestrator"
---

<objective>Run strict live production-path acceptance, all gates, protected/allowlist checks, then make the sole Requirement 2 commit and push.</objective>

<execution_context>@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-CAPABILITY-EVIDENCE.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-PLANNING-STATUS.revised.md
@.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-COVERAGE-EVIDENCE.md

<decision_coverage>
D-01–D-04/D-13: Task 1 full; D-16: Task 3 full; D-17: Tasks 1-3 full; D-18: Task 2 re-verifies full. Voice/avatar/WebRTC remain fail-closed, never live-implemented.
</decision_coverage>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove behavior and two correlated IQ calls on the production path</name>
  <files>backend/tests/integration/test_phase31_production_text_acceptance.py, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.revised.md</files>
  <behavior>
    - Blocking sanitized preflight resolves an existing/new disposable test application Session using exact project `ai-coach-demo`, Agent `Dr-Chen-Jun`, version `5`, exact immutable Skill version, credentials, SDK versions, IQ inputs, and cleanup capability.
    - Invoke two turns through production `session_turn_orchestrator`/Session persistence, not direct probe helpers. A and B use different revisions/steps and produce distinct required behavior.
    - Each response independently correlates exact successful `knowledge_base_retrieve` call ID to its response ID; no tools override.
    - Persisted audits show same server-owned Conversation, revisions N/N+1, exact pin, digests, response IDs, and monotonic next-turn progression.
    - End Session and confirm Conversation deletion/closed state. No Agent writes; definition/tool and version-inventory fingerprints and protected hashes match.
    - Zero skip: missing prerequisite or failed cleanup fails the gate. Voice/avatar stay BLOCKED 404 and WebRTC FAIL-CLOSED; acceptance asserts production gates return those errors without external calls/fallback.
  </behavior>
  <action>Create a strict opt-in integration test that exercises real production services with a transactionally isolated/disposable application Session and guaranteed cleanup. Redact directives, IQ values, credentials, raw context, and tokenized URLs. Record only nonsecret IDs/digests/call IDs. Reuse the capability probe's read-only Agent fingerprint logic without modifying Phase 30 evidence. Fail on any fallback, Agent write, wrong pin, missing IQ correlation, audit mismatch, cleanup failure, or protected hash drift.</action>
  <verify><automated>Set-Location backend; $env:PHASE31_PRODUCTION_ACCEPTANCE='1'; .\.venv\Scripts\python.exe -m pytest tests/integration/test_phase31_production_text_acceptance.py -m integration -vv -rs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated></verify>
  <done>Strict live production evidence records A/B behavior, two correlated exact IQ calls, durable audit/progression, exact pin, no writes/tools/fallback, cleanup, and matching hashes.</done>
</task>
<task type="auto">
  <name>Task 2: Run every offline, coverage, migration, Playwright, and integrity gate</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.revised.md</files>
  <action>Before commit, record branch, baseline HEAD/origin SHA, baseline commit count, `git status --short`, explicit changed-file allowlist, and SHA-256/size manifests for `.planning/debug/*`, `backend/storage/db-backups/*`, all DB/sidecars, and Phase 30 acceptance/summary evidence. Then stop on first failure: full backend Ruff check/format; Alembic one-head and migration roundtrip; full backend pytest with configured coverage; 100% Python changed-code diff; full frontend `npm run test:coverage` with branches >=82 and unchanged config; 100% executable TS/TSX diff; `tsc -b`; production build; full Playwright and explicit Phase 31 spec with nonzero pass/zero fail/skip; Task 1 live production acceptance rerun exactly once if not already final; existing Phase 30 IQ non-regression without editing its evidence; `git diff --check`; forbidden sweep for top-level instructions+agent, previous_response_id on Session path, tools override, Agent writes, browser authority, and fallback; protected hash equality. Record exact commands/counts/percentages/nonsecret IDs. No waiver.</action>
  <verify><automated>Set-Location backend; .\.venv\Scripts\ruff.exe check .; .\.venv\Scripts\ruff.exe format --check .; .\.venv\Scripts\python.exe -m pytest -v --cov=app --cov-report=xml:coverage.xml; .\.venv\Scripts\diff-cover.exe coverage.xml --compare-branch=HEAD --fail-under=100; Set-Location ..\frontend; npm run test:coverage; npx tsc -b; npm run build; npx playwright test --config=e2e/playwright.config.ts --project=chromium; Set-Location ..; git diff --check</automated></verify>
  <done>Acceptance records all green gates, frontend branches >=82, 100% changed code, zero-skipped required E2E/live tests, and byte-identical protected work.</done>
</task>
<task type="auto">
  <name>Task 3: Stage explicit allowlist, create one commit, push once, and record receipt</name>
  <files>.planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-ACCEPTANCE.revised.md, .planning/phases/31-session-skill-temporary-context-pinned-foundry-agent/31-09-SUMMARY.revised.md</files>
  <action>Only after Tasks 1-2 are green, stage each Requirement 2 production/test/migration/planning artifact by explicit path. Never broad-add, clean, reset, or stash. Compare `git diff --cached --name-only` exactly to the recorded allowlist; reject debug/backups/DB/sidecars/coverage/logs/Phase 30 acceptance-summary. Inspect cached diff and run cached check. Create exactly one commit `feat: apply Session Skill context through Foundry Conversation`; prove baseline..HEAD count is one. Push exactly once to the current approved branch; verify remote SHA equals local. Append the post-push receipt/remote SHA to acceptance and create the summary after push, leaving those receipt lines uncommitted; never amend or create a second commit. Recheck protected hashes. If push auth/network fails, stop and record failure without retrying another branch or commit.</action>
  <verify><automated>$local=git rev-parse HEAD; $branch=git branch --show-current; $remote=git rev-parse "origin/$branch"; if ($local -ne $remote) { exit 1 }; $bad=git diff --cached --name-only | Select-String '(^\.planning/debug/|^backend/storage/db-backups/|\.db(-shm|-wal)?$|30-ACCEPTANCE\.md$|30-.*SUMMARY\.md$|coverage|\.log$)'; if ($bad) { exit 1 }</automated></verify>
  <done>One allowlisted Requirement 2 commit exists, one push occurred, remote equals local, protected files match, and post-push receipt is truthfully uncommitted.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Control |
|---|---|
| Production Session → live Foundry | Exact pin, Conversation lifecycle, correlated IQ proof |
| Working tree → commit | Explicit allowlist and protected hash manifest |
| Local → remote | One push and SHA equality |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-31-09-01 | Spoofing | Live target | mitigate | Exact project/name/version preflight and response evidence |
| T-31-09-02 | Repudiation | Behavior/IQ claim | mitigate | A/B plus two response/call-correlated terminal events |
| T-31-09-03 | Information Disclosure | Acceptance | mitigate | Sanitized IDs/digests only |
| T-31-09-04 | Tampering | Staging/protected work | mitigate | Explicit allowlist, cached audit, before/after hashes |
| T-31-09-05 | Repudiation | Release | mitigate | Baseline count, one commit, one push, remote receipt |
</threat_model>

<verification>No commit/push until every offline/E2E/live/protected gate is green. Final remote SHA equals local and only post-push receipt remains uncommitted.</verification>
<success_criteria>Requirement 2 text is production-path live-proven and released once; blocked transports remain unavailable; coverage and protected-work rules are fully satisfied.</success_criteria>
<output>Create/update the listed acceptance and summary only; no second commit or push for post-push receipts.</output>
