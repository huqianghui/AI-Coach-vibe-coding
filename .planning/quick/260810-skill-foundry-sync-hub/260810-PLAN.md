---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/skills.py
  - backend/app/services/skill_service.py
  - backend/tests/test_skills_api_foundry.py
  - frontend/src/api/skills.ts
  - frontend/src/hooks/use-skills.ts
  - frontend/src/components/shared/skill-card.tsx
  - frontend/src/pages/admin/skill-hub.tsx
  - frontend/public/locales/en-US/skill.json
  - frontend/public/locales/zh-CN/skill.json
  - frontend/e2e/skill-foundry-sync.spec.ts
autonomous: true
requirements: [SKILL-FOUNDRY-SYNC-HUB]
must_haves:
  truths:
    - "Publishing a Skill continues to upload its generated SKILL.md ZIP to Microsoft Foundry Skills Preview through client.beta.skills.create_from_files"
    - "The Skill Hub visibly reports Foundry synchronization state for every Skill"
    - "An admin can synchronize one published Skill directly from its Skill Hub card"
    - "An admin can synchronize all published, non-synced Skills from the Skill Hub"
    - "Draft and archived Skills cannot be uploaded to Foundry"
    - "Sync failures remain visible and retryable without reverting local publication"
  artifacts:
    - path: "backend/app/api/skills.py"
      provides: "Admin-only batch Foundry Skill synchronization endpoint"
    - path: "backend/app/services/skill_service.py"
      provides: "Published non-synced Skill selection and batch result summary"
    - path: "frontend/src/pages/admin/skill-hub.tsx"
      provides: "Skill Hub single and batch sync actions"
    - path: "frontend/src/components/shared/skill-card.tsx"
      provides: "Per-Skill Foundry state and synchronization action"
    - path: "frontend/e2e/skill-foundry-sync.spec.ts"
      provides: "Browser-level Skill Hub sync user-story coverage"
  key_links:
    - from: "frontend/src/pages/admin/skill-hub.tsx"
      to: "POST /api/v1/skills/batch-foundry-sync"
      via: "useBatchFoundrySync mutation"
    - from: "frontend/src/components/shared/skill-card.tsx"
      to: "POST /api/v1/skills/{skill_id}/foundry-sync"
      via: "Skill Hub retry mutation"
    - from: "backend/app/services/skill_service.py"
      to: "backend/app/services/skill_foundry_service.py"
      via: "sync_skill_to_foundry for published non-synced Skills"
---

<objective>
Complete the existing UI-authored Skill to Microsoft Foundry Skills Preview integration with an HCP-style operational experience in the Skill Hub. Reuse the already live-validated ZIP/Skills Preview upload service and add visible status, single retry, and batch synchronization without duplicating the cloud integration.
</objective>

<tasks>
1. Add an admin-only static batch synchronization route and service operation that selects only published Skills not currently synced, calls the existing Foundry Skills Preview uploader, and returns synced/failed/total counts.
2. Add typed frontend API and TanStack Query mutation support for batch synchronization.
3. Show Foundry status on every Skill card, allow single synchronization for published Skills, and add a Skill Hub “Sync to Foundry” batch action.
4. Add backend unit/API coverage and Playwright coverage for the main Skill Hub synchronization story.
5. Run Ruff, focused/full pytest as appropriate, TypeScript, build, and focused Playwright tests before commit and push.
</tasks>
